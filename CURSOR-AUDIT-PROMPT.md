# CURSOR-AUDIT-PROMPT

Legacy compatibility prompt for Cursor.

This file is not the canonical workflow authority.

## Use This Instead

For any new Cursor session on this repo:
1. Read `WORKFLOW-CODEX.md`
2. Read `HANDOFF.md` if it exists
3. Read `WORKFLOW-OFAC-PROJECT.md`
4. Read `WORKFLOW-OFAC-ELEMENT-PROCUREMENT.md` if the task includes UI, motion, sourcing, or redesign

## Audit-Only Mode

If the task is specifically an audit:
- use `DIAGNOSIS.md` as the active audit artifact if it already exists
- only create a new diagnosis file if the user explicitly asks for a fresh audit
- do not treat this file as a standing authority over the canonical workflow stack

## Cursor Compatibility Notes

- Keep the repo-local workflow self-contained
- Do not rely on missing external skill-path references
- Treat older instruction docs as archived or compatibility-only unless explicitly reactivated by the user
