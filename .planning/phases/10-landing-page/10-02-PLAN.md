---
phase: 10-landing-page
plan: 02
type: execute
wave: 2
depends_on:
  - 10-01
files_modified:
  - src/app/page.tsx
  - src/app/_components/landing/HeroSection.tsx
  - src/app/_components/landing/HowItWorksSection.tsx
  - src/app/_components/landing/FeatureStatsSection.tsx
  - src/app/_components/landing/CroweBrandedFooter.tsx
autonomous: false
requirements:
  - LAND-01
  - LAND-02
  - LAND-03
  - LAND-04
  - EXPL-04
must_haves:
  truths:
    - "User visits '/' and sees a hero section with a headline and a 'Configure Your Test' CTA button"
    - "CTA button navigates to /tool when clicked"
    - "User can read a How It Works section with 3 step cards (Configure, Run, Export)"
    - "Each How It Works step card has a 2-3 sentence methodology explanation"
    - "User can see a stats section with 4 cards: 285 SDN entries, 10 rules, 4 regions, ~53ms"
    - "User can see a Crowe-branded footer with 'Run a Test' link to /tool and 'Crowe.com' external link"
    - "next build passes with zero errors after all landing sections are created"
    - "No 'use client' directive appears in any landing section file"
  artifacts:
    - path: "src/app/page.tsx"
      provides: "Landing page root — Server Component, exports metadata, composes four sections"
      exports: ["metadata", "default LandingPage"]
      contains: "HeroSection"
    - path: "src/app/_components/landing/HeroSection.tsx"
      provides: "Hero section with headline, sub-text, and CTA Link to /tool"
      exports: ["HeroSection"]
    - path: "src/app/_components/landing/HowItWorksSection.tsx"
      provides: "3-step methodology section (LAND-02 + EXPL-04)"
      exports: ["HowItWorksSection"]
    - path: "src/app/_components/landing/FeatureStatsSection.tsx"
      provides: "4-stat grid with engine numbers (LAND-03)"
      exports: ["FeatureStatsSection"]
    - path: "src/app/_components/landing/CroweBrandedFooter.tsx"
      provides: "Rich Crowe footer with nav links (LAND-04)"
      exports: ["CroweBrandedFooter"]
  key_links:
    - from: "src/app/page.tsx"
      to: "src/app/_components/landing/HeroSection.tsx"
      via: "named import + JSX render"
      pattern: "HeroSection"
    - from: "src/app/_components/landing/HeroSection.tsx"
      to: "/tool"
      via: "Next.js Link href"
      pattern: "href=\"/tool\""
    - from: "src/app/_components/landing/FeatureStatsSection.tsx"
      to: "Phase 13 count-up animation"
      via: "className='stat-number' + data-value attribute on each stat span"
      pattern: "stat-number"
---

<objective>
Build the complete static landing page at "/" composed of four Server Component sections: HeroSection, HowItWorksSection (with EXPL-04 methodology copy), FeatureStatsSection (with real engine numbers), and CroweBrandedFooter.

Purpose: Visitors arriving at "/" see what the tool does before entering it. The landing page is the entry point for client demos — it must establish credibility and guide the user to "/tool" with a single prominent CTA.
Output: src/app/page.tsx + four _components/landing/ files. All static HTML/Tailwind, no 'use client', no animations (Phase 13).
</objective>

<execution_context>
@/c/Users/RachurA/.claude/get-shit-done/workflows/execute-plan.md
@/c/Users/RachurA/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/10-landing-page/10-RESEARCH.md
@.planning/phases/10-landing-page/10-01-SUMMARY.md
</context>

<interfaces>
<!-- Verified Tailwind v4 color tokens from src/app/globals.css @theme inline block -->
<!-- Use ONLY these token names in className attributes -->
```
bg-crowe-indigo-dark       → #011E41
bg-crowe-indigo-core       → #002E62
bg-crowe-amber             → #F5A800
bg-crowe-amber-dark        → #D7761D
text-crowe-indigo-dark     → #011E41
text-crowe-amber           → #F5A800
text-crowe-amber-bright    → #FFD231
text-crowe-tint-700        → #545968
text-crowe-tint-900        → #2d3142
bg-page                    → #f8f9fc   (warm off-white page background)
```
<!-- DO NOT use: text-crowe-indigo, bg-crowe-amber-core, text-tint-700 (not registered in @theme) -->

<!-- Tailwind v4 shadow utilities — for landing cards, use Tailwind shadow utilities: shadow-sm, shadow-md, shadow-lg -->
<!-- Or use inline style for indigo-tinted shadow: style={{ boxShadow: '0 4px 8px rgba(1,30,65,0.06), 0 1px 3px rgba(1,30,65,0.04)' }} -->

<!-- Next.js Link import (already in project) -->
```tsx
import Link from 'next/link';
// Usage: <Link href="/tool">Configure Your Test</Link>
// Usage: <a href="https://www.crowe.com" target="_blank" rel="noopener noreferrer">Crowe.com</a>
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Create landing section components and root landing page</name>
  <files>
    src/app/_components/landing/HeroSection.tsx,
    src/app/_components/landing/HowItWorksSection.tsx,
    src/app/_components/landing/FeatureStatsSection.tsx,
    src/app/_components/landing/CroweBrandedFooter.tsx,
    src/app/page.tsx
  </files>
  <action>
Create the directory src/app/_components/landing/ and write all five files. All files are Server Components — NO 'use client' in any of them.

CRITICAL constraint: Verify the @theme token names in src/app/globals.css before writing any className. Use only tokens listed in the interfaces block above.

---

FILE 1: src/app/_components/landing/HeroSection.tsx

Crowe indigo-dark hero section (full-width, centered). Headline: "Test your OFAC screening before your client does." Sub: "A live sensitivity-testing tool that degrades real-world name variations against 285 synthetic SDN entries. No file prep. No waiting." CTA button: amber background, indigo-dark text, "Configure Your Test", Link to /tool. No animations.

Visual structure:
- Outer section: bg-crowe-indigo-dark background, generous vertical padding (py-24 lg:py-32), centered text (text-center), px-6
- Container: max-w-4xl mx-auto
- h1: text-4xl md:text-5xl lg:text-6xl, text-white, font-bold, max-w-3xl, leading-tight, mx-auto
- p: text-lg, text-white/80, mt-6, max-w-xl, mx-auto
- CTA Link: mt-10 inline-flex, bg-crowe-amber, text-crowe-indigo-dark, font-bold, px-8 py-4, rounded-lg, hover:bg-crowe-amber-dark, transition-colors duration-200, shadow-lg

---

FILE 2: src/app/_components/landing/HowItWorksSection.tsx

3-step section. Each step: numbered badge (1/2/3), short title, 2-3 sentence methodology explanation. Use EXACTLY this copy verbatim:

Step 1 — Configure:
Title: "Configure"
Copy: "Set entity count, linguistic regions, and degradation rules. Each rule simulates a real-world data-entry error — typos, transliterations, dropped characters — that screening systems encounter in practice."

Step 2 — Run:
Title: "Run"
Copy: "The engine applies your selected rules to 285 synthetic SDN entries and scores each name pair using Jaro-Winkler similarity. Results are returned in under 60ms."

Step 3 — Export:
Title: "Export"
Copy: "Download a CSV of all results showing original name, degraded variant, similarity score, and match/no-match status. The catch rate tells you what percentage of degraded names your screening threshold would have flagged."

Visual structure:
- Section: bg-page (warm off-white), py-20 px-6
- Section heading: "How It Works", text-3xl font-bold text-crowe-indigo-dark, text-center, mb-12
- 3-card grid: grid grid-cols-1 md:grid-cols-3 gap-8 max-w-5xl mx-auto
- Each card: bg-white rounded-xl p-8, indigo-tinted shadow via inline style={{ boxShadow: '0 4px 8px rgba(1,30,65,0.06), 0 1px 3px rgba(1,30,65,0.04)' }}
- Step number badge: inline-flex items-center justify-center w-10 h-10 rounded-full bg-crowe-amber text-crowe-indigo-dark font-bold text-lg mb-4
- Step title: text-xl font-bold text-crowe-indigo-dark mb-3
- Step copy: text-crowe-tint-700 text-base leading-relaxed

---

FILE 3: src/app/_components/landing/FeatureStatsSection.tsx

4 stat cards with engine numbers. Define as a typed array inside the component (not exported):

const STATS = [
  { value: 285, suffix: '', label: 'Synthetic SDN Entries', sublabel: 'fully synthetic test dataset' },
  { value: 10, suffix: '', label: 'Degradation Rules', sublabel: 'covering real-world screening failures' },
  { value: 4, suffix: '', label: 'Linguistic Regions', sublabel: 'Arabic, CJK, Cyrillic, Latin' },
  { value: 53, suffix: 'ms', label: 'Avg. Processing Time', sublabel: 'worst-case on full dataset' },
];

CRITICAL for Phase 13 compatibility: each stat number span MUST have className containing "stat-number" AND a data-value attribute set to String(stat.value). Display format: value 53 shows as "~53ms", others show as plain number.

Visual structure:
- Section: bg-crowe-indigo-dark, py-20 px-6
- Section heading: "By the Numbers", text-3xl font-bold text-white text-center mb-12
- 4-card grid: grid grid-cols-2 lg:grid-cols-4 gap-6 max-w-4xl mx-auto
- Each card: rounded-xl p-8 text-center bg-white/10
- stat-number span: stat-number class + text-5xl font-bold text-crowe-amber block
- label: text-white font-semibold mt-2 text-sm
- sublabel: text-white/60 text-xs mt-1

---

FILE 4: src/app/_components/landing/CroweBrandedFooter.tsx

Rich Crowe footer. bg-crowe-indigo-dark background. Three-column grid (grid grid-cols-1 md:grid-cols-3 gap-8) inside a max-w-5xl mx-auto container, py-12 px-6:
- Left column: "Crowe" (text-white font-bold text-xl) + "OFAC Sensitivity Testing Tool" (text-white/70 text-sm mt-1)
- Center column: flex flex-col gap-3. "Run a Test" as Next.js Link to /tool. "Crowe.com" as external anchor tag (href="https://www.crowe.com" target="_blank" rel="noopener noreferrer"). Both: text-white/70 hover:text-crowe-amber transition-colors text-sm
- Right column: "For demonstration purposes only. Not for compliance use." text-white/60 text-sm

Bottom bar: border-t border-white/10 mt-8 pt-6 text-center. "© 2026 Crowe LLP. All rights reserved." text-white/50 text-xs

---

FILE 5: src/app/page.tsx

Server Component. No 'use client'. Exports metadata. Composes the four sections in order.

```tsx
import type { Metadata } from 'next';
import { HeroSection } from './_components/landing/HeroSection';
import { HowItWorksSection } from './_components/landing/HowItWorksSection';
import { FeatureStatsSection } from './_components/landing/FeatureStatsSection';
import { CroweBrandedFooter } from './_components/landing/CroweBrandedFooter';

export const metadata: Metadata = {
  title: 'OFAC Sensitivity Testing — Crowe',
  description: 'Run live OFAC sensitivity testing demonstrations with synthetic data. No file prep required.',
};

export default function LandingPage() {
  return (
    <main className="min-h-screen bg-page">
      <HeroSection />
      <HowItWorksSection />
      <FeatureStatsSection />
      <CroweBrandedFooter />
    </main>
  );
}
```

Note: layout.tsx renders the slim indigo header above this main — the landing page does not need its own header.
  </action>
  <verify>
    <automated>cd "/c/Users/RachurA/OneDrive - Crowe LLP/VS Code Programming Projects/OFAC Sensitivity Testing" && NODE_TLS_REJECT_UNAUTHORIZED=0 npm run build 2>&1 | tail -10</automated>
  </verify>
  <done>Five files created. next build exits 0. No 'use client' in any landing file. stat-number class + data-value present in FeatureStatsSection. Exact methodology copy verbatim in HowItWorksSection.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual verification of landing page and tool regression check</name>
  <files></files>
  <action>Run `NODE_TLS_REJECT_UNAUTHORIZED=0 npm run dev` and follow the how-to-verify steps to inspect all landing sections and confirm /tool has no regressions.</action>
  <what-built>
    Complete static landing page at "/" with:
    - HeroSection (indigo-dark bg, headline, amber CTA button)
    - HowItWorksSection (3 step cards with methodology copy)
    - FeatureStatsSection (4 stat cards: 285/10/4/~53ms on dark bg)
    - CroweBrandedFooter (3-column rich footer)
    The tool remains at "/tool" with no regressions.
  </what-built>
  <how-to-verify>
    Run dev server: `NODE_TLS_REJECT_UNAUTHORIZED=0 npm run dev`

    1. Visit http://localhost:3000/
       - Hero: dark indigo background fills the top section
       - Headline reads "Test your OFAC screening before your client does."
       - Amber CTA button "Configure Your Test" is visible

    2. Click "Configure Your Test"
       - URL changes to http://localhost:3000/tool
       - Tool form loads normally — parameter cards visible

    3. Back to http://localhost:3000/ — scroll down
       - How It Works: 3 cards on white/off-white background (Configure / Run / Export)
       - Each card has a numbered amber badge and a multi-sentence methodology paragraph

    4. Continue scrolling
       - Stats section: dark indigo background, 4 stat cards with amber numbers
       - Verify "285", "10", "4", "~53ms" are visible

    5. Scroll to bottom
       - Crowe footer: "Crowe" wordmark, "Run a Test" link, "Crowe.com" link
       - Disclaimer: "For demonstration purposes only. Not for compliance use."

    6. Verify no double-footer: only ONE footer at the bottom of the landing page

    7. Visit http://localhost:3000/tool directly
       - Tool form loads (no regression)
       - Slim footer visible at bottom: "Back to overview" link
  </how-to-verify>
  <resume-signal>Type "approved" if all sections render correctly, or describe any issues found</resume-signal>
  <verify>User types "approved" after visual inspection confirms all 7 checks pass</verify>
  <done>All landing sections visible at "/". CTA navigates to "/tool". Tool has no regressions. No double-footer. User has typed "approved".</done>
</task>

</tasks>

<verification>
After Wave 2 completes:
- `next build` exits 0
- "/" renders landing page (hero + 3-step + stats + footer)
- "/tool" renders tool form with no regressions
- Zero 'use client' in any _components/landing/ file
- FeatureStatsSection spans have className containing "stat-number" and data-value attributes
- HowItWorksSection contains exact verbatim methodology copy for all 3 steps
- No double-footer on landing page
- npm test (existing suite) still passes
</verification>

<success_criteria>
- LAND-01: "/" has hero + "Configure Your Test" CTA that navigates to /tool
- LAND-02: How It Works 3-step section visible with Configure/Run/Export steps
- LAND-03: Stats section shows 285, 10, 4, ~53ms — real engine numbers
- LAND-04: CroweBrandedFooter renders with "Run a Test" and "Crowe.com" links
- EXPL-04: Each How It Works step card has a 2-3 sentence methodology explanation (verbatim copy from research)
- Build: next build passes, npm test passes, zero TypeScript errors
</success_criteria>

<output>
After completion, create `.planning/phases/10-landing-page/10-02-SUMMARY.md` following the summary template.
</output>
