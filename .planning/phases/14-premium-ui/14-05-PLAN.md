---
phase: 14-premium-ui
plan: 05
type: execute
wave: 2
depends_on:
  - 14-01
files_modified:
  - src/app/tool/page.tsx
autonomous: false
requirements:
  - UIPOL-04

must_haves:
  truths:
    - "All 4 parameter form cards (Entity Counts, Linguistic Regions, Degradation Rules, Client Name) show an amber radial gradient spotlight that follows the cursor"
    - "The spotlight is purely visual — form inputs, checkboxes, and labels inside all 4 cards remain fully interactive"
    - "The form-card className is on SpotlightCard's outermost div — the Phase 13 stagger animation still targets and animates all 4 cards on page load"
    - "The card surface appearance matches the existing Crowe light card style (not the default dark neutral-900 that SpotlightCard ships with)"
  artifacts:
    - path: "src/app/tool/page.tsx"
      provides: "Tool page with SpotlightCard replacing shadcn Card for all 4 form cards"
      contains: "SpotlightCard"
  key_links:
    - from: "src/app/tool/page.tsx"
      to: "src/components/ui/spotlight-card.tsx"
      via: "import SpotlightCard from '@/components/ui/spotlight-card'"
      pattern: "SpotlightCard"
    - from: "Phase 13 stagger (createScope .form-card)"
      to: "SpotlightCard outermost div"
      via: "className='form-card' on SpotlightCard — same selector, same element structure"
      pattern: "form-card"
---

<objective>
Replace the 4 shadcn Card components in tool/page.tsx with SpotlightCard for UIPOL-04: amber cursor-following radial gradient spotlight across each form card surface.

Purpose: The form cards should feel premium — a subtle warm amber light follows the cursor across each card like a desk lamp following your hand. Form inputs and checkboxes remain fully interactive.
Output: Updated tool/page.tsx where all 4 `<Card className="form-card">` are replaced with `<SpotlightCard className="form-card ...">`.
</objective>

<execution_context>
@C:/Users/RachurA/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/RachurA/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/14-premium-ui/14-CONTEXT.md
@.planning/phases/14-premium-ui/14-RESEARCH.md
@.planning/phases/14-premium-ui/14-01-SUMMARY.md

@src/app/tool/page.tsx

<interfaces>
<!-- From Plan 01 — src/components/ui/spotlight-card.tsx -->
```tsx
interface SpotlightCardProps extends React.PropsWithChildren {
  className?: string;
  spotlightColor?: `rgba(${number}, ${number}, ${number}, ${number})`;  // default: 'rgba(255,255,255,0.25)'
}
// Renders: <div ref={divRef} ... className={`relative overflow-hidden ${className}`}>
//   children (interactive — no pointer-events interference)
//   <div className="pointer-events-none absolute inset-0 ..." />  ← spotlight overlay
// NOTE: dark styles (bg-neutral-900 border border-neutral-800) have been REMOVED
//       from the component source in Plan 01. className prop fully controls surface.
export default SpotlightCard;
```

<!-- Phase 13 stagger scope (from tool/page.tsx — must be preserved) -->
```tsx
const toolRoot = useRef<HTMLDivElement>(null);
// ...
scope.current = createScope({ root: toolRoot }).add(() => {
  animate('.form-card', { opacity: [0, 1], translateY: [50, 0], duration: 700, delay: stagger(80), ease: 'outQuint' });
});
// Selector '.form-card' must exist on SpotlightCard's outermost div
```

<!-- shadcn Card components remain as children — only Card wrapper is replaced -->
<!-- CardHeader, CardTitle, CardDescription, CardContent, CardFooter stay unchanged -->
```tsx
import { Card, CardHeader, CardTitle, CardDescription, CardContent, CardFooter } from '@/components/ui/card';
// Card import is removed; CardHeader/Title/Description/Content/Footer stay
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace all 4 Card wrappers with SpotlightCard in tool/page.tsx</name>
  <files>src/app/tool/page.tsx</files>
  <action>
Update `src/app/tool/page.tsx` to replace all 4 `<Card className="form-card">` elements with `<SpotlightCard>`.

**Import changes:**
1. Remove `Card` from the shadcn card import — keep `CardHeader, CardTitle, CardDescription, CardContent, CardFooter`
2. Add: `import SpotlightCard from '@/components/ui/spotlight-card';`

**Updated import line:**
```tsx
// BEFORE:
import {
  Card,
  CardHeader,
  CardTitle,
  CardDescription,
  CardContent,
  CardFooter,
} from '@/components/ui/card';

// AFTER:
import {
  CardHeader,
  CardTitle,
  CardDescription,
  CardContent,
  CardFooter,
} from '@/components/ui/card';
import SpotlightCard from '@/components/ui/spotlight-card';
```

**Replace all 4 Card wrappers:**

The pattern is identical for all 4 cards. Replace every:
```tsx
<Card className="form-card">
  ...
</Card>
```
with:
```tsx
<SpotlightCard
  className="form-card rounded-xl bg-card text-card-foreground shadow-crowe-sm"
  spotlightColor="rgba(245,168,0,0.08)"
>
  ...
</SpotlightCard>
```

**className explanation:**
- `form-card` — required by Phase 13 stagger selector (must be first class or anywhere — selector finds any element with this class)
- `rounded-xl` — matches existing Card rounded style
- `bg-card` — shadcn CSS variable for card background (light surface, maps to `--card` in globals.css)
- `text-card-foreground` — shadcn CSS variable for card text color
- `shadow-crowe-sm` — Crowe-branded soft indigo shadow (from Tailwind config: `0 1px 3px rgba(1,30,65,0.06), 0 1px 2px rgba(1,30,65,0.04)`)
- `spotlightColor="rgba(245,168,0,0.08)"` — amber spotlight at 8% opacity (subtle, not a disco effect)

**The 4 cards to update (all in the left panel div):**
1. `{/* Card 1 — Entity Counts */}` — `<Card className="form-card">` → `<SpotlightCard className="form-card rounded-xl bg-card text-card-foreground shadow-crowe-sm" spotlightColor="rgba(245,168,0,0.08)">`
2. `{/* Card 2 — Linguistic Regions */}` — same replacement
3. `{/* Card 3 — Degradation Rules */}` — same replacement
4. `{/* Card 4 — Client Name */}` — same replacement

**Do NOT change:**
- CardHeader, CardTitle, CardDescription, CardContent, CardFooter — all remain as children inside SpotlightCard
- The toolRoot ref and createScope stagger logic — untouched
- The right panel — no changes there
- ResultsTable, EngineExplanationPanel, Tabs — all untouched
- The Run Test Button inside CardFooter — untouched

**Verification that form-card selector still works:**
SpotlightCard renders as `<div className="relative overflow-hidden form-card rounded-xl bg-card text-card-foreground shadow-crowe-sm">`. The Phase 13 `animate('.form-card', ...)` selector finds this div — same as before, just a different underlying element type (SpotlightCard div vs shadcn Card div).
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -10</automated>
  </verify>
  <done>tool/page.tsx imports SpotlightCard (not Card), all 4 form cards are SpotlightCard with form-card className and spotlightColor="rgba(245,168,0,0.08)". CardHeader/Title/Description/Content/Footer remain as children. npm run build passes. npm test passes (no regression in form logic).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: Visual verification of all 4 UIPOL requirements</name>
  <action>Run npm run dev and verify all 4 UIPOL requirements in the browser per the how-to-verify checklist.</action>
  <verify><automated>npm run build 2>&1 | tail -5</automated></verify>
  <done>All 4 UIPOL requirements visually confirmed by human reviewer — approved signal received</done>
  <what-built>
All 4 Phase 14 UIPOL requirements implemented:
- UIPOL-01: BlurText word-by-word hero headline entrance + subtitle opacity fade
- UIPOL-02: StatTiltCard 3D hover depth on stats cards + bg-white/15 surface
- UIPOL-03: Always-on amber glow on CTA button + hover breathing amplification
- UIPOL-04: SpotlightCard amber cursor spotlight on all 4 tool form cards
  </what-built>
  <how-to-verify>
Run `npm run dev` and open http://localhost:3000

**Check 1 — UIPOL-01 BlurText hero:**
- Watch the hero headline — words should blur-to-sharp one by one (~90ms apart)
- After the last word clears, the subtitle paragraph ("A live sensitivity-testing tool...") should fade in
- Inspect the h1 element in DevTools — confirm tag is `<h1>` (not `<p>`)

**Check 2 — UIPOL-02 TiltCard stats:**
- Scroll down to "By the Numbers" section
- Hover each stat card — card should tilt 3D in the direction of the cursor (~5–7 degrees max)
- Verify count-up numbers still animate (285, 10, 4, 53 count up from 0)
- Card surface should be slightly brighter than before (bg-white/15 vs old bg-white/10)

**Check 3 — UIPOL-03 CTA glow:**
- At rest: CTA button should show a warm amber box-shadow glow without hovering
- Hover the button: glow should intensify and pulse
- Move mouse away: glow should restore to the base amber level (not disappear)

**Check 4 — UIPOL-04 SpotlightCard form:**
- Open http://localhost:3000/tool
- Move cursor across each of the 4 form cards — an amber spotlight should follow the cursor
- Click inputs and check checkboxes — all should respond normally (spotlight is visual-only)
- On page load, all 4 cards should still stagger in sequentially (Phase 13 stagger intact)
  </how-to-verify>
  <resume-signal>Type "approved" if all 4 checks pass, or describe which check failed and what you observed</resume-signal>
</task>

</tasks>

<verification>
- `npm test` — existing test suite green throughout
- `npm run build` — TypeScript clean
- Manual visual checkpoint confirms all 4 UIPOL requirements working in browser
</verification>

<success_criteria>
SpotlightCard wraps all 4 form cards with amber spotlight. form-card className preserved. Phase 13 stagger intact. All 4 UIPOL requirements verified in browser by human visual checkpoint.
</success_criteria>

<output>
After completion, create `.planning/phases/14-premium-ui/14-05-SUMMARY.md`
</output>
