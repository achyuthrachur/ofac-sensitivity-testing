---
phase: 13-animation-pass
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - src/app/_components/landing/FeatureStatsSection.tsx
  - src/app/_components/landing/HowItWorksSection.tsx
  - src/app/_components/landing/HeroSection.tsx
  - src/app/tool/page.tsx
autonomous: true
requirements: [ANIM-01, ANIM-02, ANIM-03, ANIM-04]

must_haves:
  truths:
    - "animejs is installed and importable in the project"
    - "FeatureStatsSection renders each stat number in a sub-span with data-value, separate from prefix and suffix spans"
    - "HowItWorksSection step cards each carry the how-it-works-card className"
    - "HeroSection CTA Link carries the cta-button className"
    - "tool/page.tsx 4 Cards each carry the form-card className"
  artifacts:
    - path: "package.json"
      provides: "animejs in dependencies"
      contains: "animejs"
    - path: "src/app/_components/landing/FeatureStatsSection.tsx"
      provides: "prefix/value/suffix sub-span structure with stat-value class and data-value attribute"
      contains: "stat-value"
    - path: "src/app/_components/landing/HowItWorksSection.tsx"
      provides: "how-it-works-card className on each step card"
      contains: "how-it-works-card"
    - path: "src/app/_components/landing/HeroSection.tsx"
      provides: "cta-button className on the CTA Link"
      contains: "cta-button"
    - path: "src/app/tool/page.tsx"
      provides: "form-card className on all 4 Card components"
      contains: "form-card"
  key_links:
    - from: "AnimationShells (Plan 13-02)"
      to: ".how-it-works-card selector"
      via: "className added in this plan"
      pattern: "how-it-works-card"
    - from: "AnimationShells (Plan 13-02)"
      to: ".stat-value selector"
      via: "sub-span structure added in this plan"
      pattern: "stat-value"
    - from: "tool/page.tsx animation (Plan 13-03)"
      to: ".form-card selector"
      via: "className added in this plan"
      pattern: "form-card"
---

<objective>
Install animejs and add all prerequisite CSS className selectors and markup structure that animation code in Plans 13-02 and 13-03 will target.

Purpose: Animation code cannot be written until the library exists and the DOM elements have the classNames and data attributes that Anime.js selectors will use. This plan creates the foundation — no animation logic yet, just the install + markup prep.
Output: animejs in node_modules, FeatureStatsSection sub-span structure, how-it-works-card classNames, cta-button className, form-card classNames.
</objective>

<execution_context>
@C:/Users/RachurA/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/RachurA/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-animation-pass/13-CONTEXT.md
@.planning/phases/13-animation-pass/13-RESEARCH.md
</context>

<interfaces>
<!-- Current FeatureStatsSection STATS array — executor needs to understand the data shape to add prefix/suffix correctly -->
<!-- From src/app/_components/landing/FeatureStatsSection.tsx -->

Current STATS array shape:
```typescript
const STATS = [
  { value: 285, suffix: '', label: 'Synthetic SDN Entries', sublabel: '...' },
  { value: 10,  suffix: '', label: 'Degradation Rules',     sublabel: '...' },
  { value: 4,   suffix: '', label: 'Linguistic Regions',    sublabel: '...' },
  { value: 53,  suffix: 'ms', label: 'Avg. Processing Time', sublabel: '...' },
];
```

Current rendering (single span, all in one):
```tsx
<span
  className="stat-number text-5xl font-bold text-crowe-amber block mt-2"
  data-value={String(stat.value)}
>
  {stat.value === 53 ? `~${stat.value}${stat.suffix}` : `${stat.value}${stat.suffix}`}
</span>
```

Target rendering after this plan (sub-spans for targeted count-up):
```tsx
<span className="stat-number text-5xl font-bold text-crowe-amber block mt-2">
  {stat.value === 53 && <span className="stat-prefix">~</span>}
  <span className="stat-value" data-value={String(stat.value)}>
    {stat.value}
  </span>
  {stat.suffix && <span className="stat-suffix">{stat.suffix}</span>}
</span>
```

Why: Plan 13-02 will animate `.stat-value` innerHTML from 0 to data-value. The prefix (~) and suffix (ms) must be in separate sibling spans so they remain visible throughout the count-up animation.

<!-- Current HowItWorksSection card div — needs how-it-works-card className -->
Current card div:
```tsx
<div
  key={step.number}
  className="bg-white rounded-xl p-8"
  style={{ boxShadow: '0 4px 8px rgba(1,30,65,0.06), 0 1px 3px rgba(1,30,65,0.04)' }}
>
```
Target:
```tsx
<div
  key={step.number}
  className="how-it-works-card bg-white rounded-xl p-8"
  style={{ boxShadow: '0 4px 8px rgba(1,30,65,0.06), 0 1px 3px rgba(1,30,65,0.04)' }}
>
```

<!-- Current HeroSection CTA Link — needs cta-button className -->
Current:
```tsx
<Link
  href="/tool"
  className="mt-10 inline-flex items-center gap-1 bg-crowe-amber text-crowe-indigo-dark font-bold px-8 py-4 rounded-lg hover:bg-crowe-amber-dark transition-colors duration-200 shadow-lg"
>
```
Target: add `cta-button` to className.

<!-- Current tool/page.tsx Cards — need form-card className -->
4 Card components: Entity Counts, Linguistic Regions, Degradation Rules, Client Name.
Each <Card> needs className="form-card" added.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Install animejs</name>
  <files>package.json</files>
  <action>
Run the following install command (NODE_TLS_REJECT_UNAUTHORIZED=0 is required for the Crowe corporate TLS proxy — omitting it will cause a certificate error):

```bash
NODE_TLS_REJECT_UNAUTHORIZED=0 npm install animejs
```

Confirm animejs appears in package.json dependencies after install. Do NOT install any React wrapper packages for anime.js (react-animejs-wrapper etc.) — they are v3-only and incompatible with v4. The raw animejs package is the correct install.
  </action>
  <verify>
    <automated>node -e "require('animejs')" && echo "animejs importable"</automated>
  </verify>
  <done>animejs appears in package.json dependencies and can be required from node</done>
</task>

<task type="auto">
  <name>Task 2: Prerequisite markup — classNames and FeatureStatsSection sub-spans</name>
  <files>
    src/app/_components/landing/FeatureStatsSection.tsx,
    src/app/_components/landing/HowItWorksSection.tsx,
    src/app/_components/landing/HeroSection.tsx,
    src/app/tool/page.tsx
  </files>
  <action>
Make the following targeted markup changes. These are pure additions — no logic changes, no layout changes.

**FeatureStatsSection.tsx — split stat-number into sub-spans:**

Add a `prefix` field to the STATS array for the "~" prefix (only the 53ms stat uses it). Update the STATS type inline:

```typescript
const STATS = [
  { value: 285, prefix: '',  suffix: '',   label: 'Synthetic SDN Entries',  sublabel: 'fully synthetic test dataset' },
  { value: 10,  prefix: '',  suffix: '',   label: 'Degradation Rules',      sublabel: 'covering real-world screening failures' },
  { value: 4,   prefix: '',  suffix: '',   label: 'Linguistic Regions',     sublabel: 'Arabic, CJK, Cyrillic, Latin' },
  { value: 53,  prefix: '~', suffix: 'ms', label: 'Avg. Processing Time',   sublabel: 'worst-case on full dataset' },
];
```

Replace the single `<span className="stat-number ...">` rendering with sub-spans:

```tsx
<span className="stat-number text-5xl font-bold text-crowe-amber block mt-2">
  {stat.prefix && <span className="stat-prefix">{stat.prefix}</span>}
  <span className="stat-value" data-value={String(stat.value)}>
    {stat.value}
  </span>
  {stat.suffix && <span className="stat-suffix">{stat.suffix}</span>}
</span>
```

Remove the old ternary `stat.value === 53 ? ...` rendering — it is replaced by the prefix field.

**HowItWorksSection.tsx — add how-it-works-card className:**

The step card `<div>` currently has `className="bg-white rounded-xl p-8"`. Change to:
```tsx
className="how-it-works-card bg-white rounded-xl p-8"
```

Do not remove the inline `style` boxShadow — Plan 13-02 will animate it with mouseenter/mouseleave.

**HeroSection.tsx — add cta-button className:**

The CTA `<Link>` currently has `className="mt-10 inline-flex items-center gap-1 bg-crowe-amber ..."`. Add `cta-button` to the start of that className string:
```tsx
className="cta-button mt-10 inline-flex items-center gap-1 bg-crowe-amber ..."
```

**tool/page.tsx — add form-card className to all 4 Cards:**

Add `className="form-card"` to each of the 4 `<Card>` components in the left panel:
- Card 1 (Entity Counts): `<Card className="form-card">`
- Card 2 (Linguistic Regions): `<Card className="form-card">`
- Card 3 (Degradation Rules): `<Card className="form-card">`
- Card 4 (Client Name): `<Card className="form-card">`

No other changes to tool/page.tsx at this point — animation logic comes in Plan 13-03.
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>
    - `next build` passes with no TypeScript errors
    - FeatureStatsSection renders prefix/stat-value/suffix sub-spans (grep confirms "stat-value" in FeatureStatsSection.tsx)
    - HowItWorksSection cards have how-it-works-card className
    - HeroSection CTA has cta-button className
    - tool/page.tsx all 4 Cards have form-card className
  </done>
</task>

</tasks>

<verification>
- `npm run build` passes (no TypeScript errors, no missing import errors)
- `npm run test -- --run` passes (existing engine tests unaffected)
- `node -e "require('animejs')"` exits 0
- `grep "stat-value" src/app/_components/landing/FeatureStatsSection.tsx` returns a match
- `grep "how-it-works-card" src/app/_components/landing/HowItWorksSection.tsx` returns a match
- `grep "cta-button" src/app/_components/landing/HeroSection.tsx` returns a match
- `grep "form-card" src/app/tool/page.tsx` returns 4 matches
</verification>

<success_criteria>
animejs is installed and importable. All four markup files have been updated with the className selectors and sub-span structure that animation code in Plans 13-02 and 13-03 will target. Build and test suite remain green.
</success_criteria>

<output>
After completion, create `.planning/phases/13-animation-pass/13-01-SUMMARY.md`
</output>
