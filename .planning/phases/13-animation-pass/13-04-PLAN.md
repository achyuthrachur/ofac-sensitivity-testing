---
phase: 13-animation-pass
plan: 04
type: execute
wave: 3
depends_on: [13-02, 13-03]
files_modified: []
autonomous: false
requirements: [ANIM-01, ANIM-02, ANIM-03, ANIM-04]

must_haves:
  truths:
    - "All four ANIM requirements are visually verified in a browser"
    - "No console errors from Anime.js or missing imports"
    - "Scroll reveals play once only — no replay on scroll-up"
  artifacts: []
  key_links: []
---

<objective>
Visual verification checkpoint. All animation work from Plans 13-01 through 13-03 is complete. A human verifies that all four ANIM requirements work correctly in a running browser before the phase is marked complete.

Purpose: Animation behavior cannot be verified by automated tests in a vitest node environment. This checkpoint is the quality gate for all motion work in Phase 13.
Output: Human approval that all animations work as specified.
</objective>

<execution_context>
@C:/Users/RachurA/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/RachurA/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/13-animation-pass/13-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Start dev server and run final build + test gate</name>
  <files></files>
  <action>
Run the final automated gate before handing off to the visual checkpoint:

```bash
npm run build && npm run test -- --run
```

Both must pass with zero errors. If either fails, stop and fix before proceeding to the checkpoint.

Then start the dev server for visual inspection:
```bash
npm run dev
```

Report the local URL (typically http://localhost:3000).
  </action>
  <verify>
    <automated>npm run build && npm run test -- --run</automated>
  </verify>
  <done>Build passes, all existing tests pass, dev server running.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
Phase 13 animation pass is complete. The following animations were implemented:

- ANIM-01: Landing page sections (How It Works, Stats) animate into view with scroll-triggered reveals using Anime.js onScroll + onEnterForward
- ANIM-02: The 4 parameter form cards at /tool stagger in sequentially on page load with 80ms between cards
- ANIM-03: Stats numbers on the landing page count up from 0 to their target value when the stats section scrolls into view
- ANIM-04: CTA "Configure Your Test" button breathes an amber glow on hover; How It Works step cards lift on hover
  </what-built>
  <how-to-verify>
Open http://localhost:3000 in a browser with DevTools Console open.

**Check 1 — Console clean:**
Verify zero console errors or warnings related to Anime.js or missing imports.

**Check 2 — How It Works scroll reveal (ANIM-01):**
1. Load the landing page. If How It Works is already visible, scroll down past it and back up.
2. Scroll down to the "How It Works" section.
3. Expected: The 3 step cards animate in left-to-right with a ~90ms stagger between each card. Each card fades in and rises from below.
4. Scroll back up past the section, then scroll down again.
5. Expected: Cards do NOT re-animate (play-once behavior).

**Check 3 — Stats count-up (ANIM-03):**
1. Scroll down to the "By the Numbers" section (dark indigo background).
2. Expected: The stat numbers count up from 0 to their final values — 285, 10, 4, and ~53ms. The "~" prefix and "ms" suffix should remain visible throughout the count.
3. The count-up should take approximately 1.8 seconds with a dramatic deceleration at the end (outExpo feel — fast start, slow finish).

**Check 4 — CTA button glow (ANIM-04):**
1. On the landing page hero, hover the "Configure Your Test" amber button.
2. Expected: The button shows a continuous amber glow that pulses in and out (breathing) as long as the cursor is on the button.
3. Move the cursor off the button.
4. Expected: The glow fades out quickly.

**Check 5 — How It Works card hover lift (ANIM-04):**
1. Hover each of the 3 step cards in the How It Works section.
2. Expected: Each card lifts slightly (translateY up ~5px) and its shadow deepens on hover.
3. Move cursor off the card.
4. Expected: Card returns to its original position.

**Check 6 — Form card stagger at /tool (ANIM-02):**
1. Navigate to http://localhost:3000/tool.
2. Expected: The 4 configuration cards (Entity Counts, Linguistic Regions, Degradation Rules, Client Name) animate in sequentially from bottom to top position with opacity fade, each ~80ms after the previous.
3. Hard-refresh the page (Ctrl+Shift+R).
4. Expected: The stagger replays on every page load.

**Check 7 — No regressions:**
1. Run a test from /tool — fill a client name, click Run Test.
2. Expected: Results table works as before with no visual glitches.
  </how-to-verify>
  <resume-signal>Type "approved" if all animations work as described. Describe any issues found if they need fixing.</resume-signal>
</task>

</tasks>

<verification>
Human confirmed all 4 ANIM requirements are visually working. No console errors. Scroll-reveal plays once only.
</verification>

<success_criteria>
Human approves all 6 animation checks. Phase 13 is complete when this checkpoint passes.
</success_criteria>

<output>
After completion, create `.planning/phases/13-animation-pass/13-04-SUMMARY.md`
</output>
