---
phase: 07-polish-and-deployment
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/globals.css
  - src/app/layout.tsx
  - src/app/page.tsx
  - src/components/ResultsTable.tsx
autonomous: true
requirements: []

must_haves:
  truths:
    - "npm run build completes with exit code 0 and no font/network errors"
    - "Page background renders as warm off-white (#f8f9fc), not pure white"
    - "Primary text renders as warm dark slate (#2d3142), not pure black"
    - "Slim indigo header (Crowe wordmark left, OFAC Sensitivity Testing right) appears at top of page"
    - "Minimal indigo footer with 2026 Crowe LLP disclaimer appears at bottom of page"
    - "Run Test button is amber (#F5A800) with dark indigo text, amber glow on hover"
    - "Score column shows caught rows in teal (#05AB8C) and missed rows in coral (#E5376B)"
    - "The 'N results generated' placeholder paragraph is removed from the page"
    - "All 114 existing tests remain green after CSS and JSX changes"
  artifacts:
    - path: "src/app/globals.css"
      provides: "Crowe brand tokens in @theme inline + :root override; Geist vars removed; card shadow rule; font-family rule"
      contains: "--color-crowe-indigo-dark: #011E41"
    - path: "src/app/layout.tsx"
      provides: "Geist imports removed; header + footer JSX added; metadata preserved"
      contains: "bg-crowe-indigo-dark"
    - path: "src/app/page.tsx"
      provides: "Amber Run Test button; 'N results generated' paragraph removed"
      contains: "bg-crowe-amber"
    - path: "src/components/ResultsTable.tsx"
      provides: "Score cell color-coded by row.caught"
      contains: "text-crowe-teal"
  key_links:
    - from: "src/app/globals.css @theme inline"
      to: "src/app/layout.tsx header/footer JSX"
      via: "bg-crowe-indigo-dark utility class"
      pattern: "--color-crowe-indigo-dark"
    - from: "src/app/globals.css @theme inline"
      to: "src/app/page.tsx Run Test button"
      via: "bg-crowe-amber utility class"
      pattern: "--color-crowe-amber"
    - from: "src/app/globals.css @theme inline"
      to: "src/components/ResultsTable.tsx score cell"
      via: "text-crowe-teal / text-crowe-coral utility classes"
      pattern: "--color-crowe-teal"
    - from: "src/app/globals.css :root"
      to: "shadcn Card, Button, Input, Checkbox, Label components"
      via: "CSS variable consumption"
      pattern: "--primary: #011E41"
---

<objective>
Fix the build-blocking Google Fonts import, apply the full Crowe brand design system, add a branded header and footer, and polish the two UX rough edges (amber Run Test button, score column color-coding).

Purpose: The app is fully functional but styled with Next.js defaults. This plan makes it ready for a client-facing demo with Crowe visual identity. A passing build is also required before deployment can proceed.
Output: Four modified files — globals.css (brand tokens), layout.tsx (header + footer), page.tsx (amber button + removed placeholder), ResultsTable.tsx (color-coded score column).
</objective>

<execution_context>
@C:/Users/RachurA/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/RachurA/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-polish-and-deployment/07-CONTEXT.md
@.planning/phases/07-polish-and-deployment/07-RESEARCH.md
</context>

<interfaces>
<!-- Extracted from current source files — executor uses these directly. -->

From src/app/globals.css (current structure to replace):
```css
@import "tailwindcss";
@import "tw-animate-css";
@import "shadcn/tailwind.css";

@custom-variant dark (&:is(.dark *));

@theme inline {
  --color-background: var(--background);
  --color-foreground: var(--foreground);
  --font-sans: var(--font-geist-sans);   /* ← REPLACE with 'Helvetica Neue', Arial, system-ui, sans-serif */
  --font-mono: var(--font-geist-mono);   /* ← REPLACE with 'JetBrains Mono', 'Fira Code', Consolas, monospace */
  /* ... rest of existing @theme lines (sidebar, chart, ring, input, border, etc.) ... */
}

:root {
  --radius: 0.625rem;   /* ← REPLACE entire :root block with Crowe hex values */
  --background: oklch(1 0 0);
  /* ... all current oklch values ... */
}

.dark { /* ← leave unchanged — class-based, never triggers without 'dark' on <html> */ }

@layer base {
  * { @apply border-border outline-ring/50; }
  body { @apply bg-background text-foreground; }
}
```

From src/app/layout.tsx (current — lines to remove):
```tsx
import { Geist, Geist_Mono } from "next/font/google";  // ← REMOVE this import
// ...
const geistSans = Geist({ variable: "--font-geist-sans", subsets: ["latin"] });  // ← REMOVE
const geistMono = Geist_Mono({ variable: "--font-geist-mono", subsets: ["latin"] });  // ← REMOVE
// ...
<body className={`${geistSans.variable} ${geistMono.variable} antialiased`}>  // ← simplify to className="antialiased"
```

From src/app/page.tsx (current Run Test button and placeholder):
```tsx
// Current Run Test button (line ~227-243):
<Button type="button" onClick={handleSubmit} disabled={isPending} className="w-full">

// Current placeholder to REMOVE entirely (lines ~246-250):
{result?.ok && (
  <p className="text-center text-sm text-muted-foreground">
    {rows.length} result{rows.length !== 1 ? 's' : ''} generated
  </p>
)}
```

From src/components/ResultsTable.tsx (current score cell — line 153-155):
```tsx
<td className="px-3 py-2 text-sm font-mono">
  {Math.round(row.similarityScore * 100)}% {row.caught ? '✓' : '✗'}
</td>
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Fix build-blocking Geist font import</name>
  <files>src/app/layout.tsx, src/app/globals.css</files>
  <action>
    STEP 1 — src/app/layout.tsx:
    - Remove the line: `import { Geist, Geist_Mono } from "next/font/google";`
    - Remove the `const geistSans = Geist(...)` declaration (3 lines)
    - Remove the `const geistMono = Geist_Mono(...)` declaration (3 lines)
    - Change `<body className={`${geistSans.variable} ${geistMono.variable} antialiased`}>` to `<body className="antialiased">`
    - Preserve the `export const metadata` block exactly as-is (title and description are already correct)
    - Do NOT add the header/footer yet — that is Task 3

    STEP 2 — src/app/globals.css:
    - In the `@theme inline { }` block, replace the two lines:
      `--font-sans: var(--font-geist-sans);`
      `--font-mono: var(--font-geist-mono);`
      with:
      `--font-sans: 'Helvetica Neue', Arial, system-ui, sans-serif;`
      `--font-mono: 'JetBrains Mono', 'Fira Code', Consolas, monospace;`
    - Leave all other `@theme inline` lines unchanged

    WHY: `next/font/google` fetches font subsets from `fonts.googleapis.com` at build time. The Crowe corporate TLS inspection proxy blocks this connection and causes `npm run build` to fail. Removing the import and substituting a plain CSS font-family stack eliminates the network dependency entirely. The metadata title is already correct from a prior phase.
  </action>
  <verify>
    <automated>cd "C:\Users\RachurA\OneDrive - Crowe LLP\VS Code Programming Projects\OFAC Sensitivity Testing" && NODE_TLS_REJECT_UNAUTHORIZED=0 npm run build 2>&1 | tail -10</automated>
  </verify>
  <done>npm run build exits 0 with no errors mentioning "googleapis", "google fonts", TLS, CERT_UNTRUSTED, or network. The .next/ build directory is created.</done>
</task>

<task type="auto">
  <name>Task 2: Apply Crowe brand tokens to globals.css</name>
  <files>src/app/globals.css</files>
  <action>
    STEP 1 — Extend the existing `@theme inline { }` block:
    Append the following new lines inside the existing `@theme inline { }` block (after the existing lines, before the closing `}`). Do NOT remove any existing lines — only add new ones (font-sans and font-mono were already replaced in Task 1):

    ```css
    /* Crowe brand color utilities — registered here so Tailwind generates utility classes */
    --color-crowe-indigo-dark: #011E41;
    --color-crowe-indigo-core: #002E62;
    --color-crowe-indigo-bright: #003F9F;
    --color-crowe-amber: #F5A800;
    --color-crowe-amber-dark: #D7761D;
    --color-crowe-amber-bright: #FFD231;
    --color-crowe-teal: #05AB8C;
    --color-crowe-coral: #E5376B;
    --color-crowe-tint-900: #2d3142;
    --color-crowe-tint-700: #545968;
    --color-crowe-tint-50: #f6f7fa;
    --color-page: #f8f9fc;
    ```

    CRITICAL: Every `--color-*` entry in `@theme inline` is what makes `bg-crowe-indigo-dark`, `text-crowe-amber`, etc. work as Tailwind utility classes. If a color is only in `:root` but not `@theme inline`, Tailwind will not generate the utility class and the class will silently have no effect.

    STEP 2 — Replace the entire `:root { }` block with Crowe hex values:
    Delete all lines from `:root {` through the closing `}` and replace with:

    ```css
    :root {
      --radius: 0.75rem;
      --background: #f8f9fc;
      --foreground: #2d3142;
      --card: #ffffff;
      --card-foreground: #2d3142;
      --popover: #ffffff;
      --popover-foreground: #2d3142;
      --primary: #011E41;
      --primary-foreground: #f6f7fa;
      --secondary: #f0f2f8;
      --secondary-foreground: #011E41;
      --muted: #f0f2f8;
      --muted-foreground: #545968;
      --accent: #F5A800;
      --accent-foreground: #011E41;
      --destructive: #E5376B;
      --border: #dfe1e8;
      --input: #dfe1e8;
      --ring: #002E62;
      --chart-1: #F5A800;
      --chart-2: #05AB8C;
      --chart-3: #011E41;
      --chart-4: #54C0E8;
      --chart-5: #B14FC5;
      --sidebar: #f8f9fc;
      --sidebar-foreground: #2d3142;
      --sidebar-primary: #011E41;
      --sidebar-primary-foreground: #f6f7fa;
      --sidebar-accent: #f0f2f8;
      --sidebar-accent-foreground: #011E41;
      --sidebar-border: #dfe1e8;
      --sidebar-ring: #002E62;
    }
    ```

    WHY: shadcn/ui reads `--background`, `--primary`, `--card`, etc. at runtime and applies them to every Button, Card, Input, Checkbox, and Label. Changing `:root` values re-skins the entire component library to Crowe brand without touching any individual component.

    NOTE on hex vs oklch: Tailwind v4 and shadcn accept hex values directly in `:root` and `@theme inline`. No oklch conversion is needed. The RESEARCH.md anti-patterns section explicitly warns against manual oklch conversion.

    STEP 3 — Add Crowe card shadow rule to `@layer base { }`:
    Inside the existing `@layer base { }` block, after the `body` rule, add:

    ```css
    /* Crowe card shadow — indigo-tinted, borderless floating cards */
    [data-slot="card"] {
      box-shadow:
        0 1px 3px rgba(1, 30, 65, 0.04),
        0 6px 16px rgba(1, 30, 65, 0.04),
        0 12px 32px rgba(1, 30, 65, 0.02);
      border: none;
    }
    ```

    WHY: shadcn/ui v3.8.5 adds `data-slot="card"` to its Card root element. This selector safely targets only shadcn Cards. The indigo-tinted shadow (not pure black) is the Crowe brand "floating card" effect described in CLAUDE.md Section 2.4. `border: none` removes the default shadcn card border so the soft shadow defines the card edge instead.

    LEAVE UNCHANGED: The `.dark { }` block — it is class-based, requires an explicit `dark` class on `<html>`, and will never activate since there is no dark mode toggle in this app.
  </action>
  <verify>
    <automated>cd "C:\Users\RachurA\OneDrive - Crowe LLP\VS Code Programming Projects\OFAC Sensitivity Testing" && NODE_TLS_REJECT_UNAUTHORIZED=0 npm run build 2>&1 | tail -5 && npm test 2>&1 | tail -10</automated>
  </verify>
  <done>npm run build exits 0. npm test shows all 114 tests green (no regressions). The globals.css file contains --color-crowe-indigo-dark, --primary: #011E41, --background: #f8f9fc, and [data-slot="card"] shadow rule.</done>
</task>

<task type="auto">
  <name>Task 3: Add header/footer to layout.tsx, amber button to page.tsx, color-code score column in ResultsTable.tsx</name>
  <files>src/app/layout.tsx, src/app/page.tsx, src/components/ResultsTable.tsx</files>
  <action>
    CHANGE 1 — src/app/layout.tsx: Add header and footer
    Replace the RootLayout function body so it matches this exactly (font imports and Geist vars were removed in Task 1):

    ```tsx
    export default function RootLayout({
      children,
    }: Readonly<{ children: React.ReactNode }>) {
      return (
        <html lang="en">
          <body className="antialiased">
            {/* Slim Crowe indigo header */}
            <header className="bg-crowe-indigo-dark px-6 py-3 flex items-center justify-between">
              <span className="text-white font-bold text-lg tracking-tight">Crowe</span>
              <span className="text-white/80 text-sm">OFAC Sensitivity Testing</span>
            </header>

            {children}

            {/* Minimal Crowe indigo footer */}
            <footer className="bg-crowe-indigo-dark px-6 py-4 text-center">
              <p className="text-white/70 text-xs">
                &copy; 2026 Crowe LLP. For demonstration purposes only.
              </p>
            </footer>
          </body>
        </html>
      );
    }
    ```

    The `bg-crowe-indigo-dark` utility works because Task 2 registered `--color-crowe-indigo-dark: #011E41` inside `@theme inline`. Do NOT add `'use client'` — these are static elements with no state or event handlers.

    CHANGE 2 — src/app/page.tsx: Amber Run Test button + remove placeholder paragraph
    a) Find the `<Button>` in `<CardFooter>` (currently `className="w-full"`) and change it to:
    ```tsx
    <Button
      type="button"
      onClick={handleSubmit}
      disabled={isPending}
      className="w-full bg-crowe-amber text-crowe-indigo-dark font-semibold hover:bg-crowe-amber-dark hover:shadow-[0_4px_16px_rgba(245,168,0,0.30)] transition-all"
    >
    ```
    The `bg-crowe-amber` and `text-crowe-indigo-dark` utilities both depend on Task 2's `@theme inline` registrations.

    b) Remove the entire placeholder paragraph block (do not leave empty lines where it was):
    ```tsx
    {/* DELETE these lines entirely: */}
    {result?.ok && (
      <p className="text-center text-sm text-muted-foreground">
        {rows.length} result{rows.length !== 1 ? 's' : ''} generated
      </p>
    )}
    ```
    The `<ResultsTable rows={rows} clientName={clientName} />` that follows stays untouched.

    CHANGE 3 — src/components/ResultsTable.tsx: Color-code the score column cell
    Find the score `<td>` (currently `className="px-3 py-2 text-sm font-mono"`) and replace it with:
    ```tsx
    <td
      className={`px-3 py-2 text-sm font-mono ${
        row.caught ? 'text-crowe-teal' : 'text-crowe-coral'
      }`}
    >
      {Math.round(row.similarityScore * 100)}% {row.caught ? '✓' : '✗'}
    </td>
    ```
    The `text-crowe-teal` and `text-crowe-coral` utilities depend on Task 2's `@theme inline` registrations. This is a text color change only — no background color change on the cell.

    Do NOT touch the sticky `<thead className="sticky top-0 z-10 bg-white">` — `bg-white` is intentionally correct here (matches the white card background, not the off-white page background).
  </action>
  <verify>
    <automated>cd "C:\Users\RachurA\OneDrive - Crowe LLP\VS Code Programming Projects\OFAC Sensitivity Testing" && NODE_TLS_REJECT_UNAUTHORIZED=0 npm run build 2>&1 | tail -5 && npm test 2>&1 | tail -10</automated>
  </verify>
  <done>npm run build exits 0. npm test shows all 114 tests green. layout.tsx contains bg-crowe-indigo-dark and no Geist references. page.tsx contains bg-crowe-amber. ResultsTable.tsx contains text-crowe-teal. The "N results generated" paragraph is absent from page.tsx.</done>
</task>

</tasks>

<verification>
After all three tasks complete:
1. `NODE_TLS_REJECT_UNAUTHORIZED=0 npm run build` exits 0 with no font/network/TLS errors
2. `npm test` reports 114 tests passing (same count as before this phase — CSS/JSX changes cannot break pure-logic unit tests)
3. `npm run dev` serves the app; browser confirms:
   - Page background is warm off-white (visually cooler than pure white)
   - Indigo header with "Crowe" left and "OFAC Sensitivity Testing" right
   - Indigo footer with "© 2026 Crowe LLP. For demonstration purposes only."
   - Run Test button is amber with dark indigo text
   - After submitting, score column shows teal ✓ for caught rows and coral ✗ for missed rows
   - "N results generated" paragraph is gone
</verification>

<success_criteria>
- npm run build passes with exit code 0
- npm test: 114/114 tests green
- Crowe brand tokens applied — background #f8f9fc, primary text #2d3142, cards floating on soft indigo shadows
- Header: slim indigo bar, "Crowe" wordmark left, "OFAC Sensitivity Testing" right
- Footer: slim indigo bar, "© 2026 Crowe LLP. For demonstration purposes only."
- Run Test button: amber background, dark indigo text, amber glow on hover
- Score column: teal for caught, coral for missed
- "N results generated" placeholder removed
</success_criteria>

<output>
After completion, create `.planning/phases/07-polish-and-deployment/07-01-SUMMARY.md`
</output>
