---
phase: 06-results-table-and-csv-export
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - src/lib/resultsUtils.ts
  - src/lib/__tests__/resultsUtils.test.ts
autonomous: true
requirements:
  - RSLT-02
  - RSLT-03
  - EXPO-01
  - EXPO-02
  - EXPO-03

must_haves:
  truths:
    - "computeCatchRate returns correct caught/total/percent for any ResultRow array"
    - "buildCsvString produces a header row with exactly 6 named columns and one data row per ResultRow"
    - "buildCsvString outputs the Similarity Score as an integer (Math.round * 100), not a float"
    - "escapeCsvField wraps fields containing commas, double-quotes, or newlines in double-quotes and doubles embedded quotes"
    - "sanitizeClientName converts spaces to hyphens, strips non-alphanumeric characters, collapses consecutive hyphens, trims edges, and falls back to 'client' when empty"
    - "buildCsvFilename produces a string matching the pattern ofac-sensitivity-{sanitized}-YYYY-MM-DD.csv"
    - "triggerCsvDownload is exported and callable (node tests do NOT invoke it — browser-only)"
  artifacts:
    - path: "src/lib/resultsUtils.ts"
      provides: "All pure helper functions for CSV generation, filename building, and catch-rate computation"
      exports:
        - escapeCsvField
        - buildCsvString
        - sanitizeClientName
        - buildCsvFilename
        - computeCatchRate
        - triggerCsvDownload
    - path: "src/lib/__tests__/resultsUtils.test.ts"
      provides: "Vitest unit tests covering all pure helpers"
      contains: "describe('resultsUtils')"
  key_links:
    - from: "src/components/ResultsTable.tsx"
      to: "src/lib/resultsUtils.ts"
      via: "named imports"
      pattern: "from '@/lib/resultsUtils'"
---

<objective>
Install the one new npm dependency and implement all pure business-logic helpers for Phase 6 using TDD.

Purpose: `ResultsTable.tsx` (Plan 02) needs catch-rate computation, CSV string building, filename sanitization, and the download trigger. Extracting these to `resultsUtils.ts` keeps the component thin and makes every testable behavior verifiable in Vitest's node environment before any UI work begins.

Output: `src/lib/resultsUtils.ts` (all helpers), `src/lib/__tests__/resultsUtils.test.ts` (full test coverage), `@tanstack/react-virtual` installed.
</objective>

<execution_context>
@C:/Users/RachurA/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/RachurA/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@src/types/index.ts
@src/lib/constants.ts
</context>

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From src/types/index.ts:
```typescript
export interface ResultRow {
  id: string;
  originalName: string;
  entityType: 'individual' | 'business' | 'vessel' | 'aircraft';
  region: 'arabic' | 'cjk' | 'cyrillic' | 'latin';
  degradedVariant: string;
  ruleId: string;
  ruleLabel: string;
  similarityScore: number;  // Jaro-Winkler 0–1
  caught: boolean;          // score > DEFAULT_CATCH_THRESHOLD (0.85)
}
```

From src/lib/constants.ts:
```typescript
export const DEFAULT_CATCH_THRESHOLD = 0.85;
```

Vitest config: environment is `node` — no DOM APIs (Blob, URL.createObjectURL, document) available in tests.
Test include pattern: `src/**/__tests__/**/*.test.ts`
Quick run: `npx vitest run src/lib/__tests__/resultsUtils.test.ts`
Full suite: `npx vitest run`
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Wave 0 — install dependency and create RED test stub</name>
  <files>src/lib/__tests__/resultsUtils.test.ts</files>
  <action>
Run `npm install @tanstack/react-virtual` in the project directory (`C:/Users/RachurA/OneDrive - Crowe LLP/VS Code Programming Projects/OFAC Sensitivity Testing`). Confirm it appears in package.json dependencies.

Create `src/lib/__tests__/resultsUtils.test.ts` with failing test stubs (RED state — no implementation exists yet). The file must import all tested functions from `@/lib/resultsUtils` (which does not exist yet, so the import itself will fail, achieving RED).

Write test stubs covering:

1. `escapeCsvField` tests:
   - Plain string (no special chars) returned unchanged
   - String containing a comma wrapped in double-quotes
   - String containing a double-quote: `ab"cd` → `"ab""cd"` (RFC 4180)
   - String containing a newline wrapped in double-quotes
   - Number passed in returns its string representation (no wrapping if no special chars)

2. `buildCsvString` tests:
   - Empty array returns just the header row (6 columns, comma-separated, no trailing newline before data)
   - Single ResultRow produces header + 1 data row separated by `\n`
   - Score field is an integer (no decimal point) — `Math.round(0.9342 * 100)` = `93`
   - Column order: `Original Name,Entity Type,Linguistic Region,Degraded Variant,Rule Applied,Similarity Score`
   - Field containing a comma is wrapped in double-quotes in output

3. `sanitizeClientName` tests:
   - `'Acme Financial Corp'` → `'Acme-Financial-Corp'`
   - `'  Crowe LLP  '` (leading/trailing spaces) → `'Crowe-LLP'`
   - `'Client & Partners, Inc.'` (special chars stripped) → `'Client-Partners-Inc'`
   - `''` (empty string) → `'client'`
   - `'---'` (only hyphens, strip leading/trailing) → `'client'`
   - Consecutive spaces produce single hyphen, not double

4. `buildCsvFilename` tests:
   - Contains `'ofac-sensitivity-'` prefix
   - Contains sanitized clientName in the middle
   - Ends with `'.csv'`
   - Contains a date segment matching `/\d{4}-\d{2}-\d{2}/`

5. `computeCatchRate` tests:
   - Empty array → `{ caught: 0, total: 0, percent: 0 }`
   - 3 rows all caught → `{ caught: 3, total: 3, percent: 100 }`
   - 2 caught of 4 → `{ caught: 2, total: 4, percent: 50 }`
   - 1 caught of 3 → `{ caught: 1, total: 3, percent: 33 }` (Math.round)
   - Uses `row.caught` boolean directly — does NOT re-compute from `similarityScore`

Use `vi.mock` or stub `ResultRow` objects inline — create a helper `makeRow(overrides: Partial<ResultRow>): ResultRow` at the top of the test file that provides safe defaults so individual tests only set the field they care about.

Do NOT include `triggerCsvDownload` tests — it uses Blob and URL APIs unavailable in node env. Include a comment: `// triggerCsvDownload — browser-only, not testable in node env`.

Run `npx vitest run src/lib/__tests__/resultsUtils.test.ts` and confirm tests FAIL (RED) due to missing module. Commit: `test(06-01): add failing resultsUtils tests (RED)`.
  </action>
  <verify>
    <automated>npx vitest run src/lib/__tests__/resultsUtils.test.ts 2>&1 | head -30</automated>
  </verify>
  <done>Tests fail with "Cannot find module '@/lib/resultsUtils'" or equivalent module-not-found error. RED state confirmed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wave 1 — implement resultsUtils.ts (GREEN)</name>
  <files>src/lib/resultsUtils.ts</files>
  <behavior>
    - escapeCsvField: plain string unchanged; comma/double-quote/newline/CR triggers wrapping in double-quotes; embedded double-quotes doubled per RFC 4180; numbers converted via String(value)
    - buildCsvString: header always present; data rows joined with '\n'; similarityScore written as Math.round(score * 100) integer; field with comma correctly quoted
    - sanitizeClientName: trim → spaces→hyphens → strip non-[a-zA-Z0-9-] → collapse consecutive hyphens → trim leading/trailing hyphens → fallback 'client' when result is empty
    - buildCsvFilename: `ofac-sensitivity-${sanitized}-${YYYY-MM-DD}.csv` using toISOString().slice(0,10) for date
    - computeCatchRate: empty→zeros; uses row.caught boolean; percent via Math.round
    - triggerCsvDownload: prepends '\uFEFF' BOM, Blob type 'text/csv;charset=utf-8;', creates anchor, clicks, removes, revokes URL — exported but not called in tests
  </behavior>
  <action>
Create `src/lib/resultsUtils.ts` implementing all six exported functions. Use the exact signatures and logic from the research patterns:

```
escapeCsvField(value: string | number): string
buildCsvString(rows: ResultRow[]): string
sanitizeClientName(clientName: string): string
buildCsvFilename(clientName: string): string
computeCatchRate(rows: ResultRow[]): { caught: number; total: number; percent: number }
triggerCsvDownload(csvContent: string, filename: string): void  // browser-only, do not guard with typeof window check — it is only called from onClick handlers
```

Import `ResultRow` from `'@/types'`. No other imports needed — these are pure string/array operations.

CSV column header order (exact strings):
`'Original Name', 'Entity Type', 'Linguistic Region', 'Degraded Variant', 'Rule Applied', 'Similarity Score'`

`buildCsvString` uses the unsorted `rows` array (caller passes what to serialize — no sorting inside this function).

`triggerCsvDownload`:
- Prepend `'\uFEFF'` (UTF-8 BOM) to the CSV content string BEFORE Blob creation
- Blob type: `'text/csv;charset=utf-8;'` (exact string including semicolons)
- After link.click(): call document.body.removeChild(link) then URL.revokeObjectURL(url)
- Both removeChild and revokeObjectURL are required — do not omit either

After implementation, run `npx vitest run src/lib/__tests__/resultsUtils.test.ts` and confirm all tests PASS (GREEN).

Commit: `feat(06-01): implement resultsUtils pure helpers (GREEN)`.
  </action>
  <verify>
    <automated>npx vitest run src/lib/__tests__/resultsUtils.test.ts</automated>
  </verify>
  <done>All resultsUtils tests pass green. `npx vitest run` (full suite) also passes with no regressions.</done>
</task>

</tasks>

<verification>
1. `npm install @tanstack/react-virtual` completed — package.json lists it under dependencies
2. `npx vitest run src/lib/__tests__/resultsUtils.test.ts` exits 0 (all green)
3. `npx vitest run` exits 0 (no regressions in existing suites)
4. `npx run typecheck` exits 0 (resultsUtils.ts has no type errors)
5. `src/lib/resultsUtils.ts` exports all 6 functions — verify with: `grep "^export function" src/lib/resultsUtils.ts`
</verification>

<success_criteria>
- @tanstack/react-virtual present in package.json
- src/lib/__tests__/resultsUtils.test.ts exists with tests for all 5 pure helpers (not triggerCsvDownload)
- src/lib/resultsUtils.ts exports: escapeCsvField, buildCsvString, sanitizeClientName, buildCsvFilename, computeCatchRate, triggerCsvDownload
- All tests green; full suite green
- TypeScript strict mode: zero errors
</success_criteria>

<output>
After completion, create `.planning/phases/06-results-table-and-csv-export/06-01-SUMMARY.md` using the summary template.
</output>
