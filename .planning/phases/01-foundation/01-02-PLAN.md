---
phase: 01-foundation
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - src/types/index.ts
  - src/lib/constants.ts
  - src/lib/rules/.gitkeep
  - src/app/actions/.gitkeep
  - data/.gitkeep
  - components/ui/button.tsx
  - components/ui/input.tsx
  - components/ui/checkbox.tsx
  - components/ui/label.tsx
  - components/ui/card.tsx
  - src/app/globals.css
  - components.json
autonomous: false
requirements: []

must_haves:
  truths:
    - "`SdnEntry`, `ResultRow`, `RunParams`, `Region`, and `EntityType` are importable from `@/types`"
    - "`DEFAULT_CATCH_THRESHOLD` (0.85) and `MAX_ENTITY_COUNT` (500) are importable from `@/lib/constants`"
    - "`Region` and `EntityType` are union types derived from const objects — not hardcoded string literals"
    - "Directory skeleton exists: `src/lib/rules/`, `src/app/actions/`, `data/` are tracked by git via .gitkeep files"
    - "shadcn/ui is initialized and the five baseline components (button, input, checkbox, label, card) are importable"
    - "`npx tsc --noEmit` passes with zero errors after all additions"
  artifacts:
    - path: "src/types/index.ts"
      provides: "Shared type contracts: REGION_VALUES, Region, ENTITY_TYPE_VALUES, EntityType, SdnEntry, ResultRow, RunParams"
      exports:
        - REGION_VALUES
        - Region
        - ENTITY_TYPE_VALUES
        - EntityType
        - SdnEntry
        - ResultRow
        - RunParams
    - path: "src/lib/constants.ts"
      provides: "Compile-time constants: DEFAULT_CATCH_THRESHOLD, MAX_ENTITY_COUNT"
      exports:
        - DEFAULT_CATCH_THRESHOLD
        - MAX_ENTITY_COUNT
    - path: "src/lib/rules/.gitkeep"
      provides: "Placeholder so lib/rules/ directory is git-tracked"
    - path: "src/app/actions/.gitkeep"
      provides: "Placeholder so app/actions/ directory is git-tracked"
    - path: "data/.gitkeep"
      provides: "Placeholder so data/ directory is git-tracked (at project root, not src/data/)"
    - path: "components/ui/button.tsx"
      provides: "shadcn/ui Button component"
    - path: "components/ui/input.tsx"
      provides: "shadcn/ui Input component"
    - path: "components/ui/checkbox.tsx"
      provides: "shadcn/ui Checkbox component"
    - path: "components/ui/label.tsx"
      provides: "shadcn/ui Label component"
    - path: "components/ui/card.tsx"
      provides: "shadcn/ui Card component"
  key_links:
    - from: "src/types/index.ts"
      to: "REGION_VALUES const object"
      via: "type Region = (typeof REGION_VALUES)[keyof typeof REGION_VALUES]"
      pattern: "typeof REGION_VALUES"
    - from: "src/types/index.ts"
      to: "ENTITY_TYPE_VALUES const object"
      via: "type EntityType = (typeof ENTITY_TYPE_VALUES)[keyof typeof ENTITY_TYPE_VALUES]"
      pattern: "typeof ENTITY_TYPE_VALUES"
    - from: "ResultRow"
      to: "SdnEntry"
      via: "entityType: SdnEntry['entityType'], region: SdnEntry['region']"
      pattern: "SdnEntry\\['entityType'\\]"
    - from: "src/lib/constants.ts"
      to: "downstream engine and UI"
      via: "export const DEFAULT_CATCH_THRESHOLD"
      pattern: "DEFAULT_CATCH_THRESHOLD"
---

<objective>
Define the shared TypeScript type contracts (`SdnEntry`, `ResultRow`, `RunParams`, `Region`, `EntityType`) and compile-time constants (`DEFAULT_CATCH_THRESHOLD`, `MAX_ENTITY_COUNT`) that every subsequent phase imports. Initialize shadcn/ui and install the five baseline UI components. Create the directory skeleton so subsequent phases have clear landing zones for their files.

Purpose: These type contracts are the single source of truth for the entire application. Every subsequent phase (data, engine, server action, UI) imports from `@/types` — this plan establishes that contract. shadcn/ui initialization here means Phases 5 and 6 can add components without a separate setup step.
Output: `src/types/index.ts`, `src/lib/constants.ts`, `.gitkeep` files for three directories, and five shadcn/ui components.
</objective>

<execution_context>
@C:/Users/RachurA/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/RachurA/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/phases/01-foundation/01-01-SUMMARY.md

<interfaces>
<!-- Key type contracts this plan defines. All downstream phases import these. -->
<!-- Executor: implement exactly these shapes — do not add fields not listed here. -->

<!-- src/types/index.ts — implement verbatim -->
```typescript
// const object pattern — union derived, not hardcoded
export const REGION_VALUES = {
  arabic: 'arabic',
  cjk: 'cjk',
  cyrillic: 'cyrillic',
  latin: 'latin',
} as const;

export type Region = (typeof REGION_VALUES)[keyof typeof REGION_VALUES];
// Result: 'arabic' | 'cjk' | 'cyrillic' | 'latin'

export const ENTITY_TYPE_VALUES = {
  individual: 'individual',
  business: 'business',
  vessel: 'vessel',
  aircraft: 'aircraft',
} as const;

export type EntityType = (typeof ENTITY_TYPE_VALUES)[keyof typeof ENTITY_TYPE_VALUES];
// Result: 'individual' | 'business' | 'vessel' | 'aircraft'

export interface SdnEntry {
  id: string;
  name: string;
  entityType: EntityType;
  region: Region;
  country?: string;          // optional — display only
}

export interface ResultRow {
  id: string;                              // stable React key
  originalName: string;
  entityType: SdnEntry['entityType'];      // reference, not redefinition
  region: SdnEntry['region'];              // reference, not redefinition
  degradedVariant: string;
  ruleId: string;                          // machine identifier
  ruleLabel: string;                       // human-readable (e.g. "Space Removal")
  similarityScore: number;                 // Jaro-Winkler 0–1; populated Phase 4
  caught: boolean;                         // score > DEFAULT_CATCH_THRESHOLD; populated Phase 4
}

export interface RunParams {
  entityCounts: {
    individual: number;      // 0–500
    business: number;
    vessel: number;
    aircraft: number;
  };
  regions: Region[];         // which linguistic regions to sample from
  ruleIds: string[];         // which degradation rules to apply
  clientName: string;        // used in CSV filename
}
```

<!-- src/lib/constants.ts — implement verbatim -->
```typescript
/** Jaro-Winkler score above which a degraded variant is considered "caught".
 *  Standard in watchlist screening literature. Not a form parameter in v1. */
export const DEFAULT_CATCH_THRESHOLD = 0.85;

/** Maximum names per entity type the form allows. */
export const MAX_ENTITY_COUNT = 500;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create shared type contracts and constants</name>
  <files>
    src/types/index.ts
    src/lib/constants.ts
    src/lib/rules/.gitkeep
    src/app/actions/.gitkeep
    data/.gitkeep
  </files>
  <action>
**Step 1 — Create src/types/index.ts.**

Create this file with the exact type definitions shown in the `<interfaces>` block above. Do not add any imports — this file has no dependencies. Do not deviate from the field names, types, or const-object pattern specified. Inline comments from the interface block are encouraged (they explain the pattern for future phases).

```typescript
// src/types/index.ts
// Single source of truth for all shared type contracts.
// Every module in this project imports from '@/types' — never redefine these elsewhere.

export const REGION_VALUES = {
  arabic: 'arabic',
  cjk: 'cjk',
  cyrillic: 'cyrillic',
  latin: 'latin',
} as const;

export type Region = (typeof REGION_VALUES)[keyof typeof REGION_VALUES];

export const ENTITY_TYPE_VALUES = {
  individual: 'individual',
  business: 'business',
  vessel: 'vessel',
  aircraft: 'aircraft',
} as const;

export type EntityType = (typeof ENTITY_TYPE_VALUES)[keyof typeof ENTITY_TYPE_VALUES];

export interface SdnEntry {
  id: string;
  name: string;
  entityType: EntityType;
  region: Region;
  country?: string;
}

export interface ResultRow {
  id: string;
  originalName: string;
  entityType: SdnEntry['entityType'];
  region: SdnEntry['region'];
  degradedVariant: string;
  ruleId: string;
  ruleLabel: string;
  similarityScore: number;
  caught: boolean;
}

export interface RunParams {
  entityCounts: {
    individual: number;
    business: number;
    vessel: number;
    aircraft: number;
  };
  regions: Region[];
  ruleIds: string[];
  clientName: string;
}
```

**Step 2 — Create src/lib/constants.ts.**

```typescript
// src/lib/constants.ts
// Compile-time constants shared across engine and UI.
// Add all magic numbers here — never inline them in feature code.

/** Jaro-Winkler score above which a degraded variant is considered "caught".
 *  Standard in watchlist screening literature. Not a form parameter in v1. */
export const DEFAULT_CATCH_THRESHOLD = 0.85;

/** Maximum names per entity type the form allows. */
export const MAX_ENTITY_COUNT = 500;
```

**Step 3 — Create directory skeleton with .gitkeep files.**

Create these three empty files so the directories are tracked by git:

- `src/lib/rules/.gitkeep` — rule modules land here in Phase 3
- `src/app/actions/.gitkeep` — server action lands here in Phase 4
- `data/.gitkeep` — sdn.json dataset lands here in Phase 2 (at the project root level, NOT inside src/)

Note on data/ location: Per the architecture pattern, `data/sdn.json` lives at the project root (not inside src/). Create `data/.gitkeep` at: `{project_root}/data/.gitkeep`

**Step 4 — Run TypeScript check.**

Run: `npx tsc --noEmit`

Must exit 0. If it fails with "no unused locals" on the new type file (unlikely, since everything is exported), check that no intermediate unused variable was introduced. The types file exports everything at the module level — no local-only variables.
  </action>
  <verify>
    <automated>cd "/c/Users/RachurA/OneDrive - Crowe LLP/VS Code Programming Projects/OFAC Sensitivity Testing" && npx tsc --noEmit && test -f src/types/index.ts && test -f src/lib/constants.ts && test -f "src/lib/rules/.gitkeep" && test -f "src/app/actions/.gitkeep" && test -f "data/.gitkeep"</automated>
  </verify>
  <done>
    - `src/types/index.ts` exists and exports: REGION_VALUES, Region, ENTITY_TYPE_VALUES, EntityType, SdnEntry, ResultRow, RunParams
    - `src/lib/constants.ts` exists and exports DEFAULT_CATCH_THRESHOLD = 0.85 and MAX_ENTITY_COUNT = 500
    - `Region` is derived from `typeof REGION_VALUES` — NOT a bare string union
    - `EntityType` is derived from `typeof ENTITY_TYPE_VALUES` — NOT a bare string union
    - `ResultRow.entityType` is typed as `SdnEntry['entityType']` (reference, not redefinition)
    - Three .gitkeep files exist: src/lib/rules/, src/app/actions/, data/
    - `npx tsc --noEmit` exits 0
  </done>
</task>

<task type="auto">
  <name>Task 2: Initialize shadcn/ui and add baseline components</name>
  <files>
    components.json
    src/app/globals.css
    src/lib/utils.ts
    components/ui/button.tsx
    components/ui/input.tsx
    components/ui/checkbox.tsx
    components/ui/label.tsx
    components/ui/card.tsx
  </files>
  <action>
**Step 1 — Check Tailwind version from Plan 01 SUMMARY.**

Read `.planning/phases/01-foundation/01-01-SUMMARY.md` to confirm which Tailwind version create-next-app installed. This affects the shadcn/ui init flow.

**Step 2 — Run shadcn/ui init.**

Run: `npx shadcn@latest init`

When prompted, respond:
- Style: **Default** (not New York — Default is more neutral and easier to override in Phase 7)
- Base color: **Neutral** (NOT zinc, slate, or stone — neutral is easiest to override for Crowe branding in Phase 7)
- CSS variables for colors: **Yes**

If Tailwind v4 is installed (from Plan 01 summary) and shadcn/ui init fails or produces errors:
- Check the shadcn/ui docs for Tailwind v4 compatibility at execution time
- If shadcn/ui requires Tailwind v3, downgrade: `npm install tailwindcss@3 --save-dev`
- Then re-run: `npx shadcn@latest init`
- Document the downgrade in the SUMMARY

**Step 3 — Add baseline components.**

Run: `npx shadcn@latest add button input checkbox label card`

If npm ERESOLVE peer dependency errors occur (React 19 conflict), retry with:
`npx shadcn@latest add button input checkbox label card --legacy-peer-deps`

This is expected behavior with npm + React 19 + some Radix UI packages. The flag resolves metadata mismatches — it does not affect runtime behavior.

**Step 4 — Verify TypeScript compiles.**

Run: `npx tsc --noEmit`

Must exit 0. shadcn/ui components use Radix UI — they may introduce new type declarations. If tsc fails on shadcn/ui generated files:
- Check if the error is in a shadcn-generated file (components/ui/*.tsx)
- Do NOT modify generated shadcn files to suppress errors — instead check if a tsconfig option is too aggressive (e.g., noUnusedParameters flagging a Radix component's unused prop). If so, add a targeted suppress for that specific tsconfig option or scope.

**Step 5 — Run lint.**

Run: `npm run lint`

Must exit 0.

**Step 6 — Commit all Phase 1 artifacts.**

Stage and commit:

```bash
git add src/types/index.ts src/lib/constants.ts "src/lib/rules/.gitkeep" "src/app/actions/.gitkeep" data/.gitkeep components/ components.json src/lib/utils.ts src/app/globals.css
git commit -m "feat(01-foundation): define type contracts, constants, directory skeleton, and shadcn/ui baseline"
```

Note: If `src/lib/utils.ts` and `components/` were created by the shadcn init, include them. If globals.css was modified by shadcn init (CSS variables added), include it.
  </action>
  <verify>
    <automated>cd "/c/Users/RachurA/OneDrive - Crowe LLP/VS Code Programming Projects/OFAC Sensitivity Testing" && npx tsc --noEmit && npm run lint && test -f components/ui/button.tsx && test -f components/ui/input.tsx && test -f components/ui/checkbox.tsx && test -f components/ui/label.tsx && test -f components/ui/card.tsx && test -f components.json</automated>
  </verify>
  <done>
    - components.json exists (shadcn/ui configuration file)
    - components/ui/button.tsx, input.tsx, checkbox.tsx, label.tsx, card.tsx all exist
    - `npx tsc --noEmit` exits 0 (with shadcn components included in compilation)
    - `npm run lint` exits 0
    - At least one new git commit exists with Phase 1 type contracts and components
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify all Phase 1 Foundation artifacts</name>
  <what-built>
    All Phase 1 Foundation artifacts:
    - Next.js 15 scaffold with TypeScript strict mode + 5 extra compiler options
    - Shared type contracts: SdnEntry, ResultRow, RunParams, Region, EntityType
    - Constants: DEFAULT_CATCH_THRESHOLD = 0.85, MAX_ENTITY_COUNT = 500
    - Directory skeleton: src/lib/rules/, src/app/actions/, data/
    - shadcn/ui initialized with 5 baseline components (button, input, checkbox, label, card)
    - Prettier configured
    - git history clean (no node_modules/, no .docx)
  </what-built>
  <how-to-verify>
    1. Run `npm run dev` in the project root. Confirm the terminal shows "Ready" or "Local: http://localhost:3000" with no errors. Visit http://localhost:3000 in a browser — the page should load (even if it just shows "OFAC Sensitivity Testing" heading).

    2. Run `npx tsc --noEmit`. Confirm exit 0.

    3. Run `npm run lint`. Confirm exit 0.

    4. Run `git log --oneline`. Confirm at least one commit exists and the commit message does NOT mention node_modules.

    5. Run `git status`. Confirm no unexpected untracked files appear (especially no node_modules/ entries or .docx).

    6. Open `src/types/index.ts`. Verify:
       - REGION_VALUES is a const object with arabic, cjk, cyrillic, latin
       - Region type uses `typeof REGION_VALUES` derivation
       - ENTITY_TYPE_VALUES is a const object with individual, business, vessel, aircraft
       - EntityType type uses `typeof ENTITY_TYPE_VALUES` derivation
       - SdnEntry, ResultRow, RunParams interfaces are all present

    7. Open `src/lib/constants.ts`. Verify DEFAULT_CATCH_THRESHOLD = 0.85 and MAX_ENTITY_COUNT = 500 are exported.

    8. Check that these files/dirs exist:
       - components/ui/button.tsx
       - components/ui/card.tsx
       - src/lib/rules/.gitkeep
       - src/app/actions/.gitkeep
       - data/.gitkeep
  </how-to-verify>
  <action>Human verification checkpoint. All automated tasks in this plan have completed. Review the artifacts listed in what-built and follow the how-to-verify steps to confirm Phase 1 is complete.</action>
  <verify>
    <automated>cd "/c/Users/RachurA/OneDrive - Crowe LLP/VS Code Programming Projects/OFAC Sensitivity Testing" && npx tsc --noEmit && npm run lint && test -f src/types/index.ts && test -f src/lib/constants.ts && test -f components/ui/button.tsx</automated>
  </verify>
  <done>Human confirms: dev server starts, types visible in src/types/index.ts, shadcn components present, git history clean</done>
  <resume-signal>Type "approved" if all checks pass, or describe any issues found</resume-signal>
</task>

</tasks>

<verification>
Phase 1 complete when all four success criteria from ROADMAP.md are TRUE:

1. `npm run dev` starts the dev server without errors
2. `SdnEntry`, `ResultRow`, `RunParams` importable from `@/types` — verified by `npx tsc --noEmit` passing with the types present
3. ESLint and TypeScript strict mode pass with zero errors — `npx tsc --noEmit && npm run lint` both exit 0
4. Directory structure for `lib/rules/`, `app/actions/`, `data/` exists — .gitkeep files present and git-tracked

Automated gate: `npx tsc --noEmit && npm run lint` — both must exit 0.
Manual gate: Dev server starts, human-verify checkpoint approved.
</verification>

<success_criteria>
- `src/types/index.ts` exports SdnEntry, ResultRow, RunParams, Region (derived), EntityType (derived), REGION_VALUES, ENTITY_TYPE_VALUES
- `src/lib/constants.ts` exports DEFAULT_CATCH_THRESHOLD = 0.85, MAX_ENTITY_COUNT = 500
- Region and EntityType are const-object-derived unions — not bare string union literals
- shadcn/ui initialized with neutral base color and CSS variables enabled
- Five shadcn/ui components installed: button, input, checkbox, label, card
- Directory skeleton tracked by git: src/lib/rules/, src/app/actions/, data/
- `npx tsc --noEmit` exits 0 with all Phase 1 files in scope
- `npm run lint` exits 0
- Human-verify checkpoint approved
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-02-SUMMARY.md` using the summary template.
Include: Tailwind version confirmed, shadcn/ui style and base color chosen, whether --legacy-peer-deps was needed, any tsconfig adjustments made for shadcn compatibility, exact exports from types/index.ts and constants.ts.
</output>
