---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - tsconfig.json
  - next.config.ts
  - eslint.config.mjs
  - tailwind.config.ts
  - postcss.config.mjs
  - src/app/layout.tsx
  - src/app/page.tsx
  - src/app/globals.css
  - .gitignore
  - .prettierrc
  - .prettierignore
autonomous: true
requirements: []

must_haves:
  truths:
    - "Running `npm run dev` starts the Next.js dev server on port 3000 with no errors"
    - "`npx tsc --noEmit` exits with code 0 on the fresh scaffold"
    - "`npm run lint` exits with code 0 on the fresh scaffold"
    - "The project root git repo has node_modules/ and the .docx file excluded — `git status` shows neither"
    - "A .prettierrc is present with project-standard formatting rules"
  artifacts:
    - path: "package.json"
      provides: "Next.js 15 + TypeScript + Tailwind + ESLint project manifest"
    - path: "tsconfig.json"
      provides: "TypeScript strict mode config with extra safety options"
    - path: ".gitignore"
      provides: "Excludes orphan node_modules/, .env.local, .vercel/, .docx"
    - path: ".prettierrc"
      provides: "Consistent formatting: singleQuote, es5 trailing commas, printWidth 100"
    - path: ".prettierignore"
      provides: "Excludes .next/, node_modules/, data/sdn.json from formatting"
  key_links:
    - from: "tsconfig.json"
      to: "src/ directory"
      via: "paths alias @/* -> ./src/*"
      pattern: "\"@/\\*\""
    - from: ".gitignore"
      to: "project root"
      via: "node_modules/ exclusion"
      pattern: "^node_modules"
---

<objective>
Bootstrap the Next.js 15 TypeScript project in-place, configure git safety for the orphan artifacts, and establish the code-quality baseline (strict TypeScript, ESLint, Prettier) that every subsequent plan builds on.

Purpose: Without a working scaffold and clean git state, nothing else can be built or committed. This plan is the prerequisite for all other Phase 1 work.
Output: A runnable Next.js 15 project with strict TypeScript, ESLint, Prettier, and a git repo that excludes the pre-existing orphan node_modules/ and .docx file.
</objective>

<execution_context>
@C:/Users/RachurA/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/RachurA/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold Next.js 15 project in-place and fix gitignore</name>
  <files>
    package.json
    tsconfig.json
    next.config.ts
    eslint.config.mjs
    tailwind.config.ts
    postcss.config.mjs
    src/app/layout.tsx
    src/app/page.tsx
    src/app/globals.css
    .gitignore
  </files>
  <action>
The project root already exists at: C:/Users/RachurA/OneDrive - Crowe LLP/VS Code Programming Projects/OFAC Sensitivity Testing/

It contains an orphan node_modules/ (unrelated mammoth/docx package) and a file named "Sensitivity Degradation Sample Prep PDD V0.9.docx". Do NOT delete these — only exclude them from git.

**Step 1 — Run create-next-app in-place.**

From the project root, run:
```
npx create-next-app@latest . --typescript --tailwind --eslint --app --src-dir --import-alias "@/*"
```

When prompted:
- "Would you like to use Turbopack for next dev?" → YES (or No — either is fine, Claude's discretion)
- Any other prompts → accept defaults

The `.` target initializes in-place. create-next-app will ask about overwriting any existing files — accept (there are none relevant).

**Step 2 — Verify Tailwind version and record it.**

Run: `npm list tailwindcss`

If Tailwind v4 is installed, note it. Do not downgrade yet — shadcn/ui compatibility is verified in Plan 02. If v3 is installed, proceed normally.

**Step 3 — Git initialization check.**

Run: `git status`

If create-next-app already initialized git (it typically does), skip `git init`. If not, run `git init`.

**Step 4 — Harden .gitignore BEFORE any git add.**

Open the generated .gitignore. Verify or add the following entries. If .gitignore was generated by create-next-app it will likely already have node_modules/ — confirm and add missing entries:

```
# dependencies
node_modules/
.pnp
.pnp.js

# Next.js
.next/
out/

# Vercel
.vercel/

# Environment
.env.local
.env*.local

# Artifacts
*.tsbuildinfo
next-env.d.ts

# OS
.DS_Store
Thumbs.db

# Project-specific: binary design doc (no version-control value)
Sensitivity Degradation Sample Prep PDD V0.9.docx
```

**Step 5 — Verify git safety.**

Run: `git status`

Confirm that NEITHER `node_modules/` entries NOR the `.docx` file appear in the untracked files list. If they appear, the .gitignore entry is not matching — fix before continuing. Common cause: path separator or filename whitespace issue on Windows.

**Step 6 — Harden tsconfig.json.**

Open the generated tsconfig.json. Within compilerOptions, add or merge these additional strict options (keep all generated options, only ADD these if not already present):

```json
"noImplicitReturns": true,
"noFallthroughCasesInSwitch": true,
"noUnusedLocals": true,
"noUnusedParameters": true,
"forceConsistentCasingInFileNames": true
```

DO NOT add `noUncheckedIndexedAccess` — it generates false positives in data-access code.

**Step 7 — Run tsc immediately after adding strict options.**

Run: `npx tsc --noEmit`

If it fails due to "no unused locals/parameters" or similar errors in scaffold-generated files (e.g., src/app/page.tsx unused imports), fix those files by removing the unused imports. Do NOT suppress with `@ts-ignore`. The scaffold page.tsx is typically a simple component — removing or simplifying it to a minimal valid React component is acceptable:

```tsx
// src/app/page.tsx
export default function Home() {
  return (
    <main>
      <h1>OFAC Sensitivity Testing</h1>
    </main>
  );
}
```

Keep layout.tsx — just ensure it has no unused imports.

After fixing, re-run: `npx tsc --noEmit` — must exit 0.

**Step 8 — Run ESLint.**

Run: `npm run lint`

Must exit 0. If lint errors exist in scaffold files, fix them. Common cause: unused variables in generated scaffold code.
  </action>
  <verify>
    <automated>cd "/c/Users/RachurA/OneDrive - Crowe LLP/VS Code Programming Projects/OFAC Sensitivity Testing" && npx tsc --noEmit && npm run lint && git status | grep -v "node_modules\|\.docx" | head -20</automated>
  </verify>
  <done>
    - `npx tsc --noEmit` exits 0
    - `npm run lint` exits 0
    - `git status` shows neither node_modules/ nor .docx file in untracked files
    - package.json exists with next, react, react-dom, typescript dependencies
    - tsconfig.json has "strict": true plus the five additional compiler options
  </done>
</task>

<task type="auto">
  <name>Task 2: Add Prettier and make initial git commit</name>
  <files>
    .prettierrc
    .prettierignore
  </files>
  <action>
**Step 1 — Install Prettier.**

Run: `npm install --save-dev prettier`

**Step 2 — Create .prettierrc** at the project root with these exact contents:

```json
{
  "singleQuote": true,
  "trailingComma": "es5",
  "tabWidth": 2,
  "useTabs": false,
  "printWidth": 100,
  "semi": true,
  "jsxSingleQuote": false,
  "bracketSpacing": true,
  "arrowParens": "always",
  "endOfLine": "lf"
}
```

**Step 3 — Create .prettierignore** at the project root:

```
.next/
node_modules/
data/sdn.json
```

**Step 4 — Verify TypeScript and lint are still clean.**

Run: `npx tsc --noEmit && npm run lint`

Must both exit 0.

**Step 5 — Stage and commit.**

Run git status first to confirm the list of files to stage. Stage only the project files (NOT node_modules/, NOT the .docx file):

```bash
git add src/ .gitignore .prettierrc .prettierignore package.json package-lock.json tsconfig.json next.config.ts eslint.config.mjs tailwind.config.ts postcss.config.mjs public/ README.md
```

Note: If create-next-app already made an initial commit, run `git log --oneline` to check. If a commit exists already, just add the Prettier files to a new commit:
```bash
git add .prettierrc .prettierignore package.json package-lock.json
git commit -m "chore(01-foundation): add Prettier config"
```

If no prior commit exists, stage all relevant files and commit:
```bash
git commit -m "feat(01-foundation): scaffold Next.js 15 with TypeScript strict, Tailwind, ESLint, Prettier"
```

**Step 6 — Final smoke check.**

Run: `npm run dev -- --port 3001`

Observe that the server starts without errors. After confirming startup (look for "Ready" or "Local: http://localhost:3001"), stop the server (Ctrl+C or kill the process).

Record the Tailwind version installed in a comment or note for Plan 02 (shadcn/ui compatibility check).
  </action>
  <verify>
    <automated>cd "/c/Users/RachurA/OneDrive - Crowe LLP/VS Code Programming Projects/OFAC Sensitivity Testing" && npx tsc --noEmit && npm run lint && test -f .prettierrc && test -f .prettierignore && git log --oneline | head -3</automated>
  </verify>
  <done>
    - .prettierrc exists with the project-standard config
    - .prettierignore exists
    - `npx tsc --noEmit` exits 0
    - `npm run lint` exits 0
    - At least one git commit exists in the repo history
    - `npm run dev` starts the server without errors (verified by observation)
  </done>
</task>

</tasks>

<verification>
After both tasks complete:

1. `npx tsc --noEmit` exits 0 — strict TypeScript including extra options passes
2. `npm run lint` exits 0 — ESLint passes on scaffold
3. `npm run dev` starts Next.js dev server without errors (smoke check: observe "Ready" message)
4. `git status` confirms node_modules/ and .docx file are excluded
5. `git log --oneline` shows at least one commit
6. `.prettierrc` present with singleQuote, trailingComma: "es5", printWidth: 100
</verification>

<success_criteria>
- Running `npm run dev` starts the dev server without errors
- TypeScript strict mode + five additional compiler options pass with zero errors
- ESLint passes with zero errors
- git repo is initialized and the first commit does NOT include node_modules/ or .docx
- Prettier is installed and configured
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-01-SUMMARY.md` using the summary template.
Include: Tailwind version installed (v3 or v4), whether create-next-app auto-initialized git, any scaffold TS errors encountered and how they were resolved.
</output>
