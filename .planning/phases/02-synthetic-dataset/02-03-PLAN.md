---
phase: 02-synthetic-dataset
plan: 03
type: execute
wave: 3
depends_on:
  - 02-01
  - 02-02
files_modified: []
autonomous: false
requirements:
  - DATA-06

must_haves:
  truths:
    - "A compliance professional (or the planner) confirms Arabic names use ism+nasab or kunya patterns"
    - "A compliance professional confirms Chinese names appear surname-first (ZHANG WEI not WEI ZHANG)"
    - "A compliance professional confirms Russian names include three-token given+patronymic+surname structure"
    - "Phase 2 is signed off as complete and ready for Phase 3 to build against"
  artifacts:
    - path: "data/sdn.json"
      provides: "Culturally authentic SDN names verified by domain review"
      contains: '"region": "arabic"'
  key_links:
    - from: "Human domain reviewer"
      to: "data/sdn.json Arabic individuals"
      via: "Manual review against RESEARCH.md naming tables"
      pattern: "IBN|BINT|ABU|ABD AL-"
---

<objective>
Domain reviewer confirms that the naming conventions in data/sdn.json meet the cultural authenticity bar required by DATA-06 — the only requirement that cannot be automated.

Purpose: A compliance professional must eyeball that Arabic names follow ism+nasab or kunya patterns, Chinese names are surname-first, and Russian names include patronymics. TypeScript cannot validate linguistic authenticity. This checkpoint gates Phase 3 from starting until the data foundation is trusted.

Output: Human sign-off that DATA-06 is satisfied; Phase 2 declared complete.
</objective>

<execution_context>
@C:/Users/RachurA/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/RachurA/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-synthetic-dataset/02-RESEARCH.md
@.planning/phases/02-synthetic-dataset/02-02-SUMMARY.md

@data/sdn.json
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Domain review — naming authenticity (DATA-06)</name>
  <files>data/sdn.json</files>
  <action>
    Run the following three inspection commands to surface sample entries for review. Do NOT modify any files — this task is read-only. Present the output to the reviewer and wait for approval or correction requests.

    1. Arabic individuals (first 10):
       node -e "const d=require('./data/sdn.json');d.filter(e=>e.entityType==='individual'&&e.region==='arabic').slice(0,10).forEach(e=>console.log(e.name))"

    2. CJK individuals (first 10):
       node -e "const d=require('./data/sdn.json');d.filter(e=>e.entityType==='individual'&&e.region==='cjk').slice(0,10).forEach(e=>console.log(e.name))"

    3. Cyrillic individuals (first 10):
       node -e "const d=require('./data/sdn.json');d.filter(e=>e.entityType==='individual'&&e.region==='cyrillic').slice(0,10).forEach(e=>console.log(e.name))"

    Present the output and pause for the reviewer to confirm or request corrections.
  </action>
  <verify>
    <automated>node -e "const d=require('./data/sdn.json');const t=d.reduce((a,e)=>{a[e.entityType]=(a[e.entityType]||0)+1;return a},{});console.log(JSON.stringify(t));" && npx tsc --noEmit</automated>
  </verify>
  <done>Reviewer types "approved" — confirming Arabic ism+nasab/kunya patterns, surname-first CJK order, and Russian three-token patronymic structure are all present and authentic.</done>
  <what-built>
    data/sdn.json — 285 synthetic SDN entries across four entity types and four linguistic regions.
    The dataset was hand-authored using verbatim name tables from RESEARCH.md, which were in turn drawn from compliance screening literature and linguistic sources.
  </what-built>
  <how-to-verify>
    Review the sample entries produced by the action commands above and confirm they look authentic to a compliance professional:

    **Arabic individuals — confirm ism+nasab or kunya pattern:**
    Expected patterns:
    - Long nasab chains: HASSAN IBN ALI IBN OMAR AL-RASHIDI (given + ibn + father + ibn + grandfather + al-nisba)
    - Short nasab: OMAR BIN YUSUF AL-QAHTANI (given + bin + father + al-nisba)
    - Kunya (nom de guerre): ABU BAKR AL-SHAMI (Abu + son's name + al-nisba)
    - Female BINT form: FATIMA BINT ALI AL-RASHIDI (given + bint + father + al-nisba)
    - Theophoric: ABD AL-RAHIM IBN IBRAHIM AL-SURI (Abd al-attribute form)

    **CJK individuals — confirm surname-first:**
    Expected: ZHANG WEI (surname ZHANG first, given WEI second). KIM JONG SU (surname KIM first). NGUYEN VAN HUNG (family NGUYEN first).

    **Cyrillic individuals — confirm three-token given+patronymic+surname:**
    Expected: IGOR VLADIMIROVICH PETROV (given + patronymic from father Vladimir + surname). Ukrainian variants use -OVYCH/-IVNA endings.

    **What to flag if you see issues:**
    - Names that look structurally wrong (e.g., Western name order for an Arabic entry)
    - Any entry where region and naming convention clearly do not match
    - Any name that is obviously a real living sanctioned person's name (keep fictional)
  </how-to-verify>
  <resume-signal>
    Type "approved" if the naming conventions look authentic and Phase 2 is complete.
    Or describe specific entries that need correction and Claude will fix them before re-review.
  </resume-signal>
</task>

</tasks>

<verification>
Human sign-off received. After approval:
1. DATA-06 is satisfied — naming conventions confirmed authentic
2. Phase 2 is complete — all six DATA-01 through DATA-06 requirements addressed
3. Phase 3 (Transformation Engine) may begin
</verification>

<success_criteria>
- Domain reviewer confirms Arabic names follow ism+nasab or kunya conventions
- Domain reviewer confirms Chinese/Korean/Vietnamese names are surname-first
- Domain reviewer confirms Russian names include three-token patronymic structure
- No obviously incorrect name-region pairings identified
- npx tsc --noEmit still green at checkpoint time
- Phase 2 declared complete; no further changes to data/sdn.json expected before Phase 7
</success_criteria>

<output>
After completion, create `.planning/phases/02-synthetic-dataset/02-03-SUMMARY.md`
</output>
